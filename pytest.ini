[pytest]
DJANGO_SETTINGS_MODULE = config.settings.dev
python_files = test*.py
addopts = --strict-markers
markers =
    scale: Django test tag for the load-shaped suites (pytest-django maps TestCase.tags onto markers)

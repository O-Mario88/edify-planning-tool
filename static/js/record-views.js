/* Record views: all operational tables remain scrollable comparative tables across all viewports. */
(() => {
  'use strict';
  // Tables remain semantic tables on mobile devices without card transformation.
})();

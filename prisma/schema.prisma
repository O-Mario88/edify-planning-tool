generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["partialIndexes"]
}

datasource db {
  provider = "postgresql"
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model activity {
  created_at                       DateTime                          @db.Timestamptz(6)
  updated_at                       DateTime                          @db.Timestamptz(6)
  deleted_at                       DateTime?                         @db.Timestamptz(6)
  id                               String                            @id @db.VarChar(30)
  activity_type                    String                            @db.VarChar(48)
  project_id                       String?                           @db.VarChar(30)
  fy                               String                            @db.VarChar(16)
  quarter                          String                            @db.VarChar(8)
  month                            Int?
  week                             Int?
  scheduled_date                   DateTime?                         @db.Timestamptz(6)
  planned_month                    Int?
  planned_week                     Int?
  responsible_staff_id             String?                           @db.VarChar(30)
  monitored_by_staff_id            String?                           @db.VarChar(30)
  assigned_partner_id              String?                           @db.VarChar(30)
  delivery_type                    String                            @db.VarChar(16)
  cluster_slot                     String?                           @db.VarChar(16)
  purpose_intervention             String?                           @db.VarChar(64)
  status                           String                            @db.VarChar(32)
  evidence_status                  String                            @db.VarChar(16)
  salesforce_activity_id           String?                           @db.VarChar(128)
  salesforce_activity_type         String?                           @db.VarChar(16)
  ia_verification_status           String                            @db.VarChar(16)
  ia_confirmed_at                  DateTime?                         @db.Timestamptz(6)
  ia_confirmed_by                  String?                           @db.VarChar(30)
  payment_status                   String                            @db.VarChar(32)
  reschedule_count                 Int
  last_reason                      String?                           @db.VarChar(512)
  est_cost_cents                   Int
  cost_missing                     Boolean
  pl_review_note                   String?                           @db.VarChar(512)
  pl_reviewed_at                   DateTime?                         @db.Timestamptz(6)
  pl_reviewed_by                   String?                           @db.VarChar(30)
  teachers_attended                Int?
  leaders_attended                 Int?
  other_participants               Int?
  next_meeting_date                DateTime?                         @db.Timestamptz(6)
  cluster_id                       String?                           @db.VarChar(30)
  school_id                        String?                           @db.VarChar(30)
  environment                      String                            @db.VarChar(16)
  source                           String                            @db.VarChar(32)
  cluster                          cluster?                          @relation(fields: [cluster_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "activity_cluster_id_f73a0a06_fk_cluster_id")
  school                           school?                           @relation(fields: [school_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "activity_school_id_9c9b89e9_fk_school_id")
  activity_completion_verification activity_completion_verification?
  activity_schedule_cost_line      activity_schedule_cost_line[]
  advance_request                  advance_request[]
  evidence_record                  evidence_record[]

  @@index([assigned_partner_id], map: "activity_assigne_f20ef6_idx")
  @@index([cluster_id], map: "activity_cluster_1af84f_idx")
  @@index([cluster_id], map: "activity_cluster_id_f73a0a06")
  @@index([cluster_id], map: "activity_cluster_id_f73a0a06_like")
  @@index([created_at], map: "activity_created_at_07d3d9ba")
  @@index([deleted_at], map: "activity_deleted_at_6c716dfe")
  @@index([environment], map: "activity_environment_403d13a4")
  @@index([environment], map: "activity_environment_403d13a4_like")
  @@index([evidence_status], map: "activity_evidenc_78c12a_idx")
  @@index([fy, quarter], map: "activity_fy_8be759_idx")
  @@index([ia_verification_status, payment_status], map: "activity_ia_veri_4001c9_idx")
  @@index([id], map: "activity_id_d0854dbb_like")
  @@index([responsible_staff_id], map: "activity_respons_711310_idx")
  @@index([scheduled_date], map: "activity_schedul_398a42_idx")
  @@index([school_id], map: "activity_school__03d4a0_idx")
  @@index([school_id], map: "activity_school_id_9c9b89e9")
  @@index([school_id], map: "activity_school_id_9c9b89e9_like")
  @@index([source], map: "activity_source_c54d178e")
  @@index([source], map: "activity_source_c54d178e_like")
  @@index([status], map: "activity_status_ebaa20_idx")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model activity_completion_verification {
  created_at    DateTime  @db.Timestamptz(6)
  updated_at    DateTime  @db.Timestamptz(6)
  id            String    @id @db.VarChar(30)
  salesforce_id String    @db.VarChar(128)
  entered_by    String    @db.VarChar(30)
  entered_at    DateTime  @db.Timestamptz(6)
  status        String    @db.VarChar(16)
  ia_actor_id   String?   @db.VarChar(30)
  ia_action_at  DateTime? @db.Timestamptz(6)
  ia_note       String?   @db.VarChar(512)
  activity_id   String    @unique @db.VarChar(30)
  activity      activity  @relation(fields: [activity_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "activity_completion__activity_id_375f8eb5_fk_activity_")

  @@index([activity_id], map: "activity_completion_verification_activity_id_375f8eb5_like")
  @@index([created_at], map: "activity_completion_verification_created_at_c76c13e1")
  @@index([id], map: "activity_completion_verification_id_a47831d6_like")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model activity_schedule_cost_line {
  created_at           DateTime         @db.Timestamptz(6)
  updated_at           DateTime         @db.Timestamptz(6)
  id                   String           @id @db.VarChar(30)
  cost_setting_key     String           @db.VarChar(128)
  label                String           @db.VarChar(255)
  unit_cost            Int
  quantity             Int
  amount               Int
  cost_setting_version Int
  activity_id          String           @db.VarChar(30)
  catalogue_id         String?          @db.VarChar(30)
  catalogue_version    Int?
  currency             String           @db.VarChar(8)
  line_item_type       String?          @db.VarChar(64)
  activity             activity         @relation(fields: [activity_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "activity_schedule_cost_line_activity_id_494f9d33_fk_activity_id")
  advance_request      advance_request?

  @@index([activity_id], map: "activity_sc_activit_1991a7_idx")
  @@index([activity_id], map: "activity_schedule_cost_line_activity_id_494f9d33")
  @@index([activity_id], map: "activity_schedule_cost_line_activity_id_494f9d33_like")
  @@index([created_at], map: "activity_schedule_cost_line_created_at_811b3983")
  @@index([id], map: "activity_schedule_cost_line_id_6c78ba07_like")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model admin_budget_line {
  created_at               DateTime                 @db.Timestamptz(6)
  updated_at               DateTime                 @db.Timestamptz(6)
  id                       String                   @id @db.VarChar(30)
  cost_category            String                   @db.VarChar(64)
  description              String                   @db.VarChar(512)
  quantity                 Decimal                  @db.Decimal(12, 2)
  unit_cost                BigInt
  total_cost               BigInt
  justification            String?
  created_by_user_id       String                   @db.VarChar(30)
  status                   String                   @db.VarChar(32)
  monthly_budget_id        String                   @db.VarChar(30)
  monthly_work_plan_budget monthly_work_plan_budget @relation(fields: [monthly_budget_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "admin_budget_line_monthly_budget_id_43b38989_fk_monthly_w")

  @@index([monthly_budget_id], map: "admin_budge_monthly_0ed267_idx")
  @@index([created_at], map: "admin_budget_line_created_at_0249605d")
  @@index([id], map: "admin_budget_line_id_4681b06c_like")
  @@index([monthly_budget_id], map: "admin_budget_line_monthly_budget_id_43b38989")
  @@index([monthly_budget_id], map: "admin_budget_line_monthly_budget_id_43b38989_like")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model advance_request {
  created_at                  DateTime                    @db.Timestamptz(6)
  updated_at                  DateTime                    @db.Timestamptz(6)
  id                          String                      @id @db.VarChar(30)
  responsible_user_id         String?                     @db.VarChar(30)
  fy                          String                      @db.VarChar(16)
  quarter                     String                      @db.VarChar(8)
  month                       Int?
  week                        Int?
  planned_date                DateTime?                   @db.Timestamptz(6)
  amount                      BigInt
  status                      String                      @db.VarChar(40)
  advance_type                String                      @db.VarChar(16)
  disbursed_amount            BigInt?
  disbursed_at                DateTime?                   @db.Timestamptz(6)
  disbursed_by_user_id        String?                     @db.VarChar(30)
  disburse_method             String?                     @db.VarChar(64)
  disburse_reference          String?                     @db.VarChar(128)
  accounted_amount            BigInt?
  returned_amount             BigInt?
  accountability_netsuite_id  String?                     @db.VarChar(128)
  accountability_submitted_at DateTime?                   @db.Timestamptz(6)
  accountability_reviewed_at  DateTime?                   @db.Timestamptz(6)
  confirmed_at                DateTime?                   @db.Timestamptz(6)
  last_note                   String?                     @db.VarChar(512)
  activity_id                 String                      @db.VarChar(30)
  budget_line_id              String                      @unique(map: "uniq_advance_per_budget_line") @db.VarChar(30)
  activity                    activity                    @relation(fields: [activity_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "advance_request_activity_id_b315c1ce_fk_activity_id")
  activity_schedule_cost_line activity_schedule_cost_line @relation(fields: [budget_line_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "advance_request_budget_line_id_15281a13_fk_activity_")

  @@index([fy, month], map: "advance_req_fy_480aae_idx")
  @@index([responsible_user_id], map: "advance_req_respons_f97908_idx")
  @@index([status], map: "advance_req_status_c044e0_idx")
  @@index([activity_id], map: "advance_request_activity_id_b315c1ce")
  @@index([activity_id], map: "advance_request_activity_id_b315c1ce_like")
  @@index([budget_line_id], map: "advance_request_budget_line_id_15281a13")
  @@index([budget_line_id], map: "advance_request_budget_line_id_15281a13_like")
  @@index([created_at], map: "advance_request_created_at_2aa4023d")
  @@index([id], map: "advance_request_id_e3ef4d78_like")
}

model annual_plan {
  created_at                  DateTime                      @db.Timestamptz(6)
  updated_at                  DateTime                      @db.Timestamptz(6)
  id                          String                        @id @db.VarChar(30)
  fy                          String                        @db.VarChar(16)
  owner_staff_id              String?                       @db.VarChar(30)
  status                      String                        @db.VarChar(32)
  planning_annualplanactivity planning_annualplanactivity[]

  @@unique([fy, owner_staff_id], map: "uniq_annualplan_fy_owner")
  @@index([created_at], map: "annual_plan_created_at_c7ff4d79")
  @@index([id], map: "annual_plan_id_aa1150bf_like")
}

model assignment_audit {
  created_at          DateTime @db.Timestamptz(6)
  updated_at          DateTime @db.Timestamptz(6)
  id                  String   @id @db.VarChar(30)
  action              String   @db.VarChar(64)
  school_id           String?  @db.VarChar(30)
  activity_id         String?  @db.VarChar(30)
  assigner_id         String   @db.VarChar(30)
  assigner_role       String   @db.VarChar(64)
  assigned_to_type    String   @db.VarChar(16)
  assigned_staff_id   String?  @db.VarChar(30)
  assigned_partner_id String?  @db.VarChar(30)
  allowed             Boolean
  blocked_reason      String?  @db.VarChar(512)
  override_used       Boolean
  override_reason     String?  @db.VarChar(512)

  @@index([assigner_id], map: "assignment__assigne_5a09e8_idx")
  @@index([created_at], map: "assignment__created_b6a6f7_idx")
  @@index([school_id], map: "assignment__school__1c4bba_idx")
  @@index([created_at], map: "assignment_audit_created_at_5ebf0507")
  @@index([id], map: "assignment_audit_id_30ddcfa5_like")
}

model audit_log {
  id             String   @id @db.VarChar(30)
  seq            BigInt?
  action         String   @db.VarChar(128)
  subject_kind   String?  @db.VarChar(64)
  subject_id     String?  @db.VarChar(30)
  actor_id       String?  @db.VarChar(30)
  actor_role     String?  @db.VarChar(64)
  payload        Json?
  ip_address     String?  @db.VarChar(128)
  user_agent     String?  @db.VarChar(512)
  correlation_id String?  @db.VarChar(64)
  success        Boolean
  reason         String?  @db.VarChar(512)
  prev_hash      String?  @db.VarChar(128)
  hash           String?  @db.VarChar(128)
  created_at     DateTime @db.Timestamptz(6)

  @@index([action], map: "audit_log_action_b32d4d_idx")
  @@index([actor_id], map: "audit_log_actor_i_8c3cd6_idx")
  @@index([correlation_id], map: "audit_log_correla_e04bb9_idx")
  @@index([created_at], map: "audit_log_created_5a7975_idx")
  @@index([created_at], map: "audit_log_created_at_24c1f1be")
  @@index([id], map: "audit_log_id_230e0268_like")
  @@index([seq], map: "audit_log_seq_92a7c28b")
  @@index([seq], map: "audit_log_seq_f1f804_idx")
  @@index([subject_kind, subject_id], map: "audit_log_subject_9900c9_idx")
}

model auth_group {
  id                     Int                      @id @default(autoincrement())
  name                   String                   @unique @db.VarChar(150)
  auth_group_permissions auth_group_permissions[]
  user_groups            user_groups[]

  @@index([name], map: "auth_group_name_a6ea08ec_like")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model auth_group_permissions {
  id              BigInt          @id @default(autoincrement())
  group_id        Int
  permission_id   Int
  auth_permission auth_permission @relation(fields: [permission_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "auth_group_permissio_permission_id_84c5c92e_fk_auth_perm")
  auth_group      auth_group      @relation(fields: [group_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "auth_group_permissions_group_id_b120cbf9_fk_auth_group_id")

  @@unique([group_id, permission_id], map: "auth_group_permissions_group_id_permission_id_0cd325b0_uniq")
  @@index([group_id], map: "auth_group_permissions_group_id_b120cbf9")
  @@index([permission_id], map: "auth_group_permissions_permission_id_84c5c92e")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model auth_permission {
  id                     Int                      @id @default(autoincrement())
  name                   String                   @db.VarChar(255)
  content_type_id        Int
  codename               String                   @db.VarChar(100)
  auth_group_permissions auth_group_permissions[]
  django_content_type    django_content_type      @relation(fields: [content_type_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "auth_permission_content_type_id_2f476e4b_fk_django_co")
  user_user_permissions  user_user_permissions[]

  @@unique([content_type_id, codename], map: "auth_permission_content_type_id_codename_01ab375a_uniq")
  @@index([content_type_id], map: "auth_permission_content_type_id_2f476e4b")
}

model boundary_import_run {
  created_at           DateTime @db.Timestamptz(6)
  updated_at           DateTime @db.Timestamptz(6)
  id                   String   @id @db.VarChar(30)
  source_name          String   @db.VarChar(255)
  source_url           String?  @db.VarChar(1024)
  source_last_modified String?  @db.VarChar(255)
  imported_at          DateTime @db.Timestamptz(6)
  imported_by          String?  @db.VarChar(255)
  level_counts         Json
  checksum             String?  @db.VarChar(255)
  status               String   @db.VarChar(64)
  errors               Json?
  warnings             Json?

  @@index([created_at], map: "boundary_import_run_created_at_0adcf7ea")
  @@index([id], map: "boundary_import_run_id_1f8b2433_like")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model budget_intelligence_financedecisionnote {
  created_at                  DateTime                    @db.Timestamptz(6)
  updated_at                  DateTime                    @db.Timestamptz(6)
  id                          String                      @id @db.VarChar(30)
  author_user_id              String                      @db.VarChar(30)
  author_role                 String                      @db.VarChar(64)
  note                        String
  kind                        String                      @db.VarChar(32)
  insight_id                  String                      @db.VarChar(30)
  budget_intelligence_insight budget_intelligence_insight @relation(fields: [insight_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "budget_intelligence__insight_id_323e4f52_fk_budget_in")

  @@index([insight_id], map: "budget_intelligence_fina_insight_id_323e4f52_like")
  @@index([created_at], map: "budget_intelligence_financedecisionnote_created_at_5e269c5c")
  @@index([id], map: "budget_intelligence_financedecisionnote_id_b101b0b0_like")
  @@index([insight_id], map: "budget_intelligence_financedecisionnote_insight_id_323e4f52")
}

model budget_intelligence_insight {
  created_at                              DateTime                                  @db.Timestamptz(6)
  updated_at                              DateTime                                  @db.Timestamptz(6)
  id                                      String                                    @id @db.VarChar(30)
  fy                                      String                                    @db.VarChar(16)
  period_type                             String                                    @db.VarChar(16)
  period                                  String                                    @db.VarChar(32)
  insight_type                            String                                    @db.VarChar(32)
  scope_type                              String                                    @db.VarChar(16)
  scope_id                                String?                                   @db.VarChar(30)
  scope_name                              String?                                   @db.VarChar(255)
  recommendation                          String
  reason                                  String
  risk_level                              String                                    @db.VarChar(16)
  impact_yield                            String                                    @db.VarChar(16)
  confidence_level                        String                                    @db.VarChar(16)
  confidence_score                        Float
  amount_affected                         Float?
  evidence_summary                        Json?
  financial_implication                   String?                                   @db.VarChar(512)
  suggested_action                        String                                    @db.VarChar(512)
  alternatives                            Json?
  metrics                                 Json?
  risk_flags                              String[]                                  @db.VarChar(64)
  status                                  String                                    @db.VarChar(32)
  reviewed_by_user_id                     String?                                   @db.VarChar(30)
  reviewed_by_role                        String?                                   @db.VarChar(64)
  reviewed_at                             DateTime?                                 @db.Timestamptz(6)
  review_note                             String?
  generated_at                            DateTime                                  @db.Timestamptz(6)
  budget_intelligence_financedecisionnote budget_intelligence_financedecisionnote[]

  @@index([fy, insight_type], map: "budget_inte_fy_e05a41_idx")
  @@index([impact_yield], map: "budget_inte_impact__bec36b_idx")
  @@index([scope_type, scope_id], map: "budget_inte_scope_t_57f588_idx")
  @@index([status], map: "budget_inte_status_231107_idx")
  @@index([created_at], map: "budget_intelligence_insight_created_at_8e972496")
  @@index([id], map: "budget_intelligence_insight_id_0432b699_like")
}

model cd_flag {
  created_at          DateTime  @db.Timestamptz(6)
  updated_at          DateTime  @db.Timestamptz(6)
  id                  String    @id @db.VarChar(30)
  raised_by_user_id   String    @db.VarChar(30)
  raised_by_name      String?   @db.VarChar(255)
  assigned_to_user_id String    @db.VarChar(30)
  category            String    @db.VarChar(64)
  scope_type          String?   @db.VarChar(32)
  scope_id            String?   @db.VarChar(30)
  scope_name          String?   @db.VarChar(255)
  note                String
  recommended_action  String?   @db.VarChar(512)
  priority            String    @db.VarChar(16)
  due_date            String?   @db.VarChar(32)
  status              String    @db.VarChar(16)
  resolution_note     String?
  resolved_at         DateTime? @db.Timestamptz(6)

  @@index([assigned_to_user_id, status], map: "cd_flag_assigne_fb2bae_idx")
  @@index([created_at], map: "cd_flag_created_at_0c0e6095")
  @@index([id], map: "cd_flag_id_18a4d605_like")
  @@index([raised_by_user_id], map: "cd_flag_raised__095e07_idx")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model cluster {
  created_at                DateTime                    @db.Timestamptz(6)
  updated_at                DateTime                    @db.Timestamptz(6)
  deleted_at                DateTime?                   @db.Timestamptz(6)
  id                        String                      @id @db.VarChar(30)
  name                      String                      @db.VarChar(255)
  sub_county_name           String?                     @db.VarChar(255)
  cluster_type              String                      @db.VarChar(16)
  status                    String                      @db.VarChar(32)
  override_reason           String?                     @db.VarChar(512)
  responsible_staff_id      String?                     @db.VarChar(30)
  cluster_leader_name       String?                     @db.VarChar(255)
  cluster_leader_phone      String?                     @db.VarChar(64)
  district_id               String                      @db.VarChar(30)
  region_id                 String                      @db.VarChar(30)
  sub_county_id             String?                     @db.VarChar(30)
  environment               String                      @db.VarChar(16)
  source                    String                      @db.VarChar(32)
  activity                  activity[]
  district                  district                    @relation(fields: [district_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "cluster_district_id_ac1ce8b8_fk_district_id")
  region                    region                      @relation(fields: [region_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "cluster_region_id_caa68248_fk_region_id")
  sub_county                sub_county?                 @relation(fields: [sub_county_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "cluster_sub_county_id_e5df6c99_fk_sub_county_id")
  cluster_sub_county        cluster_sub_county[]
  school_cluster_assignment school_cluster_assignment[]

  @@index([created_at], map: "cluster_created_at_bd4554c1")
  @@index([deleted_at], map: "cluster_deleted_at_f4fd532f")
  @@index([district_id], map: "cluster_distric_4ea716_idx")
  @@index([district_id], map: "cluster_district_id_ac1ce8b8")
  @@index([district_id], map: "cluster_district_id_ac1ce8b8_like")
  @@index([environment], map: "cluster_environment_49e81eee")
  @@index([environment], map: "cluster_environment_49e81eee_like")
  @@index([id], map: "cluster_id_2268ff0d_like")
  @@index([region_id], map: "cluster_region_id_caa68248")
  @@index([region_id], map: "cluster_region_id_caa68248_like")
  @@index([source], map: "cluster_source_498b75f2")
  @@index([source], map: "cluster_source_498b75f2_like")
  @@index([sub_county_id], map: "cluster_sub_cou_781328_idx")
  @@index([sub_county_id], map: "cluster_sub_county_id_e5df6c99")
  @@index([sub_county_id], map: "cluster_sub_county_id_e5df6c99_like")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model cluster_sub_county {
  created_at    DateTime   @db.Timestamptz(6)
  updated_at    DateTime   @db.Timestamptz(6)
  id            String     @id @db.VarChar(30)
  cluster_id    String     @db.VarChar(30)
  sub_county_id String     @db.VarChar(30)
  cluster       cluster    @relation(fields: [cluster_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "cluster_sub_county_cluster_id_b95a83c3_fk_cluster_id")
  sub_county    sub_county @relation(fields: [sub_county_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "cluster_sub_county_sub_county_id_ea6ff494_fk_sub_county_id")

  @@unique([cluster_id, sub_county_id], map: "uniq_cluster_subcounty")
  @@index([cluster_id], map: "cluster_sub_county_cluster_id_b95a83c3")
  @@index([cluster_id], map: "cluster_sub_county_cluster_id_b95a83c3_like")
  @@index([created_at], map: "cluster_sub_county_created_at_de027ae4")
  @@index([id], map: "cluster_sub_county_id_cd0acb07_like")
  @@index([sub_county_id], map: "cluster_sub_county_sub_county_id_ea6ff494")
  @@index([sub_county_id], map: "cluster_sub_county_sub_county_id_ea6ff494_like")
  @@index([sub_county_id], map: "cluster_sub_sub_cou_972ce2_idx")
}

model command_center_alert {
  created_at                     DateTime                         @db.Timestamptz(6)
  updated_at                     DateTime                         @db.Timestamptz(6)
  id                             String                           @id @db.VarChar(30)
  alert_type                     String                           @db.VarChar(64)
  severity                       String                           @db.VarChar(16)
  scope                          String?                          @db.VarChar(64)
  context_type                   String?                          @db.VarChar(64)
  context_id                     String?                          @db.VarChar(30)
  title                          String                           @db.VarChar(255)
  body                           String?
  target_route                   String?                          @db.VarChar(255)
  condition_hash                 String                           @unique @db.VarChar(128)
  status                         String                           @db.VarChar(16)
  command_center_alert_dismissal command_center_alert_dismissal[]

  @@index([alert_type, status], map: "command_cen_alert_t_d2f61e_idx")
  @@index([status], map: "command_cen_status_896519_idx")
  @@index([condition_hash], map: "command_center_alert_condition_hash_2a8b070a_like")
  @@index([created_at], map: "command_center_alert_created_at_3831b0e7")
  @@index([id], map: "command_center_alert_id_aadddca1_like")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model command_center_alert_dismissal {
  created_at           DateTime             @db.Timestamptz(6)
  updated_at           DateTime             @db.Timestamptz(6)
  id                   String               @id @db.VarChar(30)
  user_id              String               @db.VarChar(30)
  dismissed_until      DateTime             @db.Timestamptz(6)
  alert_id             String               @db.VarChar(30)
  command_center_alert command_center_alert @relation(fields: [alert_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "command_center_alert_alert_id_2bad54a6_fk_command_c")

  @@unique([alert_id, user_id], map: "uniq_alert_dismissal_user")
  @@index([user_id], map: "command_cen_user_id_3f14af_idx")
  @@index([alert_id], map: "command_center_alert_dismissal_alert_id_2bad54a6")
  @@index([alert_id], map: "command_center_alert_dismissal_alert_id_2bad54a6_like")
  @@index([created_at], map: "command_center_alert_dismissal_created_at_fedaed8c")
  @@index([id], map: "command_center_alert_dismissal_id_50a80e5d_like")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model core_activity_slot {
  created_at             DateTime  @db.Timestamptz(6)
  updated_at             DateTime  @db.Timestamptz(6)
  id                     String    @id @db.VarChar(64)
  school_id              String    @db.VarChar(64)
  intervention           String    @db.VarChar(64)
  activity_type          String    @db.VarChar(16)
  sequence_number        Int
  status                 String    @db.VarChar(32)
  owner                  String    @db.VarChar(16)
  assigned_staff_id      String?   @db.VarChar(30)
  assigned_staff_name    String?   @db.VarChar(255)
  assigned_partner_id    String?   @db.VarChar(30)
  assigned_partner_name  String?   @db.VarChar(255)
  scheduled_month        String?   @db.VarChar(16)
  scheduled_week         Int?
  scheduled_for          String?   @db.VarChar(32)
  salesforce_id          String?   @db.VarChar(128)
  activity_id            String?   @db.VarChar(30)
  evidence_uri           String?   @db.VarChar(512)
  evidence_notes         String?
  pl_verification_status String?   @db.VarChar(16)
  ia_verification_status String?   @db.VarChar(16)
  accountant_status      String?   @db.VarChar(16)
  teachers               Int?
  leaders                Int?
  participants           Int?
  returned_reason        String?   @db.VarChar(512)
  completed_at           DateTime? @db.Timestamptz(6)
  core_plan_id           String    @db.VarChar(64)
  core_plan              core_plan @relation(fields: [core_plan_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "core_activity_slot_core_plan_id_9ffe3458_fk_core_plan_id")

  @@index([core_plan_id], map: "core_activi_core_pl_e3f0db_idx")
  @@index([school_id], map: "core_activi_school__1014ee_idx")
  @@index([core_plan_id], map: "core_activity_slot_core_plan_id_9ffe3458")
  @@index([core_plan_id], map: "core_activity_slot_core_plan_id_9ffe3458_like")
  @@index([created_at], map: "core_activity_slot_created_at_aabb1d73")
  @@index([id], map: "core_activity_slot_id_cb9b769a_like")
}

model core_candidate_verification {
  created_at       DateTime @db.Timestamptz(6)
  updated_at       DateTime @db.Timestamptz(6)
  id               String   @id @db.VarChar(30)
  school_id        String   @unique @db.VarChar(64)
  ssa_record_id    String   @db.VarChar(30)
  verification_id  String   @db.VarChar(64)
  verified_by_id   String   @db.VarChar(30)
  verified_by_name String   @db.VarChar(255)
  verified_at      DateTime @db.Timestamptz(6)
  status           String   @db.VarChar(64)
  comments         String?

  @@index([created_at], map: "core_candidate_verification_created_at_d12d8d2c")
  @@index([id], map: "core_candidate_verification_id_40cd0adf_like")
  @@index([school_id], map: "core_candidate_verification_school_id_e014f8fd_like")
}

model core_plan {
  created_at              DateTime                @db.Timestamptz(6)
  updated_at              DateTime                @db.Timestamptz(6)
  id                      String                  @id @db.VarChar(64)
  school_id               String                  @unique @db.VarChar(64)
  fy                      String                  @db.VarChar(16)
  status                  String                  @db.VarChar(32)
  visits_target           Int
  trainings_target        Int
  visits_completed        Int
  trainings_completed     Int
  baseline_average        Float?
  follow_up_average       Float?
  baseline_ssa_record_id  String?                 @db.VarChar(30)
  follow_up_ssa_record_id String?                 @db.VarChar(30)
  follow_up_scheduled_for String?                 @db.VarChar(32)
  follow_up_assignee      String?                 @db.VarChar(30)
  interventions           Json?
  created_by_id           String?                 @db.VarChar(30)
  created_by_name         String?                 @db.VarChar(255)
  core_activity_slot      core_activity_slot[]
  core_school_onboarding  core_school_onboarding?
  core_school_profile     core_school_profile?

  @@index([created_at], map: "core_plan_created_at_5b1e224e")
  @@index([id], map: "core_plan_id_afd366c3_like")
  @@index([school_id], map: "core_plan_school_id_8740ba11_like")
  @@index([status], map: "core_plan_status_75d34d_idx")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model core_school_onboarding {
  created_at             DateTime  @db.Timestamptz(6)
  updated_at             DateTime  @db.Timestamptz(6)
  id                     String    @id @db.VarChar(30)
  school_id              String    @unique @db.VarChar(64)
  fy                     String    @db.VarChar(16)
  previous_school_type   String    @db.VarChar(32)
  baseline_ssa_record_id String    @db.VarChar(30)
  baseline_average_score Float
  onboarded_by_id        String    @db.VarChar(30)
  onboarded_by_name      String    @db.VarChar(255)
  onboarded_at           DateTime  @db.Timestamptz(6)
  onboarding_reason      String?
  status                 String    @db.VarChar(32)
  core_plan_id           String    @unique @db.VarChar(64)
  core_plan              core_plan @relation(fields: [core_plan_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "core_school_onboarding_core_plan_id_a43aad4b_fk_core_plan_id")

  @@index([core_plan_id], map: "core_school_onboarding_core_plan_id_a43aad4b_like")
  @@index([created_at], map: "core_school_onboarding_created_at_e63c3513")
  @@index([id], map: "core_school_onboarding_id_7a3ac681_like")
  @@index([school_id], map: "core_school_onboarding_school_id_2801141f_like")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model core_school_profile {
  created_at      DateTime  @db.Timestamptz(6)
  updated_at      DateTime  @db.Timestamptz(6)
  id              String    @id @db.VarChar(64)
  school_id       String    @unique @db.VarChar(64)
  core_start_fy   String    @db.VarChar(16)
  champion_status String    @db.VarChar(32)
  status          String    @db.VarChar(32)
  core_plan_id    String    @unique @db.VarChar(64)
  core_plan       core_plan @relation(fields: [core_plan_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "core_school_profile_core_plan_id_58b8875e_fk_core_plan_id")

  @@index([core_plan_id], map: "core_school_profile_core_plan_id_58b8875e_like")
  @@index([created_at], map: "core_school_profile_created_at_2bdd0a00")
  @@index([id], map: "core_school_profile_id_cdf03fa1_like")
  @@index([school_id], map: "core_school_profile_school_id_c1e95221_like")
}

model cost_catalogue {
  created_at   DateTime       @db.Timestamptz(6)
  updated_at   DateTime       @db.Timestamptz(6)
  id           String         @id @db.VarChar(30)
  country      String         @db.VarChar(64)
  fy           String         @db.VarChar(16)
  version      Int
  is_active    Boolean
  label        String?        @db.VarChar(255)
  published_by String?        @db.VarChar(30)
  activated_at DateTime?      @db.Timestamptz(6)
  cost_setting cost_setting[]

  @@unique([country, fy, is_active], map: "uniq_active_catalogue_per_country_fy", where: raw("is_active"))
  @@unique([country, fy, version], map: "uniq_catalogue_country_fy_version")
  @@index([created_at], map: "cost_catalogue_created_at_ab46926e")
  @@index([id], map: "cost_catalogue_id_5c6bb325_like")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model cost_setting {
  created_at     DateTime        @db.Timestamptz(6)
  updated_at     DateTime        @db.Timestamptz(6)
  id             String          @id @db.VarChar(30)
  key            String          @unique @db.VarChar(128)
  label          String          @db.VarChar(255)
  unit_cost      BigInt
  fy             String?         @db.VarChar(16)
  version        Int
  created_by     String?         @db.VarChar(30)
  catalogue_id   String?         @db.VarChar(30)
  cost_catalogue cost_catalogue? @relation(fields: [catalogue_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "cost_setting_catalogue_id_ce188511_fk_cost_catalogue_id")

  @@index([catalogue_id], map: "cost_setting_catalogue_id_ce188511")
  @@index([catalogue_id], map: "cost_setting_catalogue_id_ce188511_like")
  @@index([created_at], map: "cost_setting_created_at_39c86ca3")
  @@index([id], map: "cost_setting_id_e629094c_like")
  @@index([key], map: "cost_setting_key_168da37b_like")
}

model cost_setting_history {
  created_at         DateTime @db.Timestamptz(6)
  updated_at         DateTime @db.Timestamptz(6)
  id                 String   @id @db.VarChar(30)
  key                String   @db.VarChar(128)
  label              String   @db.VarChar(255)
  old_unit_cost      BigInt?
  new_unit_cost      BigInt
  version            Int
  fy                 String?  @db.VarChar(16)
  changed_by_user_id String   @db.VarChar(30)
  reason             String?  @db.VarChar(512)
  changed_at         DateTime @db.Timestamptz(6)

  @@index([changed_at], map: "cost_settin_changed_5b0814_idx")
  @@index([key], map: "cost_settin_key_466339_idx")
  @@index([created_at], map: "cost_setting_history_created_at_510c2a51")
  @@index([id], map: "cost_setting_history_id_59a09fff_like")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model county {
  created_at      DateTime     @db.Timestamptz(6)
  updated_at      DateTime     @db.Timestamptz(6)
  id              String       @id @db.VarChar(30)
  name            String       @db.VarChar(255)
  normalized_name String       @db.VarChar(255)
  pcode           String?      @unique @db.VarChar(64)
  source          String?      @db.VarChar(255)
  latitude        Float?
  longitude       Float?
  district_id     String       @db.VarChar(30)
  district        district     @relation(fields: [district_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "county_district_id_b3e8fdde_fk_district_id")
  sub_county      sub_county[]

  @@unique([district_id, name], map: "uniq_county_district_name")
  @@index([created_at], map: "county_created_at_45130307")
  @@index([district_id], map: "county_district_id_b3e8fdde")
  @@index([district_id], map: "county_district_id_b3e8fdde_like")
  @@index([id], map: "county_id_b4bca749_like")
  @@index([pcode], map: "county_pcode_9b0bc7d5_like")
}

model daily_debrief {
  created_at              DateTime                  @db.Timestamptz(6)
  updated_at              DateTime                  @db.Timestamptz(6)
  deleted_at              DateTime?                 @db.Timestamptz(6)
  id                      String                    @id @db.VarChar(30)
  fy                      String                    @db.VarChar(16)
  date                    DateTime                  @db.Timestamptz(6)
  submitted_by_user_id    String                    @db.VarChar(30)
  submitted_by_role       String                    @db.VarChar(64)
  staff_id                String?                   @db.VarChar(30)
  partner_id              String?                   @db.VarChar(30)
  debrief_type            String                    @db.VarChar(16)
  status                  String                    @db.VarChar(16)
  summary                 String?
  what_happened           String?
  what_went_well          String?
  what_did_not_go_well    String?
  blockers                String[]                  @db.VarChar(255)
  blocker_other           String?                   @db.VarChar(512)
  support_needed          String?
  recommendations         String?
  next_action             String?                   @db.VarChar(512)
  linked_school_ids       String[]                  @db.VarChar(30)
  linked_cluster_ids      String[]                  @db.VarChar(30)
  linked_partner_ids      String[]                  @db.VarChar(30)
  linked_project_ids      String[]                  @db.VarChar(30)
  linked_activity_ids     String[]                  @db.VarChar(30)
  parent_debrief_id       String?                   @db.VarChar(30)
  merged_into_debrief_id  String?                   @db.VarChar(30)
  reviewed_by_user_id     String?                   @db.VarChar(30)
  reviewed_at             DateTime?                 @db.Timestamptz(6)
  review_note             String?
  submitted_at            DateTime                  @db.Timestamptz(6)
  environment             String                    @db.VarChar(16)
  source                  String                    @db.VarChar(32)
  daily_debrief_recipient daily_debrief_recipient[]

  @@index([fy, date], map: "daily_debri_fy_cfd057_idx")
  @@index([partner_id], map: "daily_debri_partner_c426ec_idx")
  @@index([status], map: "daily_debri_status_ce24c3_idx")
  @@index([submitted_by_user_id], map: "daily_debri_submitt_9102e8_idx")
  @@index([created_at], map: "daily_debrief_created_at_ad9b7bee")
  @@index([deleted_at], map: "daily_debrief_deleted_at_9c9819a9")
  @@index([environment], map: "daily_debrief_environment_c7172a6d")
  @@index([environment], map: "daily_debrief_environment_c7172a6d_like")
  @@index([id], map: "daily_debrief_id_48af0db6_like")
  @@index([source], map: "daily_debrief_source_3d991415")
  @@index([source], map: "daily_debrief_source_3d991415_like")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model daily_debrief_recipient {
  created_at        DateTime      @db.Timestamptz(6)
  updated_at        DateTime      @db.Timestamptz(6)
  id                String        @id @db.VarChar(30)
  recipient_user_id String        @db.VarChar(30)
  recipient_role    String        @db.VarChar(64)
  routing_reason    String?       @db.VarChar(255)
  action_required   Boolean
  read_at           DateTime?     @db.Timestamptz(6)
  debrief_id        String        @db.VarChar(30)
  daily_debrief     daily_debrief @relation(fields: [debrief_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "daily_debrief_recipient_debrief_id_597cb338_fk_daily_debrief_id")

  @@index([debrief_id], map: "daily_debri_debrief_466a4c_idx")
  @@index([recipient_user_id, read_at], map: "daily_debri_recipie_dd709b_idx")
  @@index([created_at], map: "daily_debrief_recipient_created_at_59f1b452")
  @@index([debrief_id], map: "daily_debrief_recipient_debrief_id_597cb338")
  @@index([debrief_id], map: "daily_debrief_recipient_debrief_id_597cb338_like")
  @@index([id], map: "daily_debrief_recipient_id_5e93ca50_like")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model district {
  created_at    DateTime     @db.Timestamptz(6)
  updated_at    DateTime     @db.Timestamptz(6)
  id            String       @id @db.VarChar(30)
  name          String       @db.VarChar(255)
  code          String?      @unique @db.VarChar(64)
  pcode         String?      @unique @db.VarChar(64)
  source        String?      @db.VarChar(255)
  latitude      Float?
  longitude     Float?
  region_id     String       @db.VarChar(30)
  sub_region_id String?      @db.VarChar(30)
  cluster       cluster[]
  county        county[]
  region        region       @relation(fields: [region_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "district_region_id_08d5327a_fk_region_id")
  sub_region    sub_region?  @relation(fields: [sub_region_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "district_sub_region_id_06a036ac_fk_sub_region_id")
  school        school[]
  sub_county    sub_county[]

  @@unique([region_id, name], map: "uniq_district_region_name")
  @@index([code], map: "district_code_27d4224b_like")
  @@index([created_at], map: "district_created_at_818e6fe4")
  @@index([id], map: "district_id_7be064e9_like")
  @@index([pcode], map: "district_pcode_13c1d5e0_like")
  @@index([region_id], map: "district_region_id_08d5327a")
  @@index([region_id], map: "district_region_id_08d5327a_like")
  @@index([sub_region_id], map: "district_sub_region_id_06a036ac")
  @@index([sub_region_id], map: "district_sub_region_id_06a036ac_like")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model django_admin_log {
  id                  Int                  @id @default(autoincrement())
  action_time         DateTime             @db.Timestamptz(6)
  object_id           String?
  object_repr         String               @db.VarChar(200)
  action_flag         Int                  @db.SmallInt
  change_message      String
  content_type_id     Int?
  user_id             String               @db.VarChar(30)
  django_content_type django_content_type? @relation(fields: [content_type_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "django_admin_log_content_type_id_c4bce8eb_fk_django_co")
  user                user                 @relation(fields: [user_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "django_admin_log_user_id_c564eba6_fk_user_id")

  @@index([content_type_id], map: "django_admin_log_content_type_id_c4bce8eb")
  @@index([user_id], map: "django_admin_log_user_id_c564eba6")
  @@index([user_id], map: "django_admin_log_user_id_c564eba6_like")
}

model django_apscheduler_djangojob {
  id                                    String                                  @id @db.VarChar(255)
  next_run_time                         DateTime?                               @db.Timestamptz(6)
  job_state                             Bytes
  django_apscheduler_djangojobexecution django_apscheduler_djangojobexecution[]

  @@index([next_run_time], map: "django_apscheduler_djangojob_next_run_time_2f022619")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model django_apscheduler_djangojobexecution {
  id                           BigInt                       @id @default(autoincrement())
  status                       String                       @db.VarChar(50)
  run_time                     DateTime                     @db.Timestamptz(6)
  duration                     Decimal?                     @db.Decimal(15, 2)
  finished                     Decimal?                     @db.Decimal(15, 2)
  exception                    String?                      @db.VarChar(1000)
  traceback                    String?
  job_id                       String                       @db.VarChar(255)
  django_apscheduler_djangojob django_apscheduler_djangojob @relation(fields: [job_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "django_apscheduler_djangojobexecution_job_id_daf5090a_fk")

  @@unique([job_id, run_time], map: "unique_job_executions")
  @@index([job_id], map: "django_apscheduler_djangojobexecution_job_id_daf5090a")
  @@index([run_time], map: "django_apscheduler_djangojobexecution_run_time_16edd96b")
}

model django_content_type {
  id               Int                @id @default(autoincrement())
  app_label        String             @db.VarChar(100)
  model            String             @db.VarChar(100)
  auth_permission  auth_permission[]
  django_admin_log django_admin_log[]

  @@unique([app_label, model], map: "django_content_type_app_label_model_76bd3d3b_uniq")
}

model django_migrations {
  id      BigInt   @id @default(autoincrement())
  app     String   @db.VarChar(255)
  name    String   @db.VarChar(255)
  applied DateTime @db.Timestamptz(6)
}

model django_session {
  session_key  String   @id @db.VarChar(40)
  session_data String
  expire_date  DateTime @db.Timestamptz(6)

  @@index([expire_date], map: "django_session_expire_date_a5c62663")
  @@index([session_key], map: "django_session_session_key_c0390e0f_like")
}

model domain_event_log {
  created_at     DateTime  @db.Timestamptz(6)
  updated_at     DateTime  @db.Timestamptz(6)
  id             String    @id @db.VarChar(30)
  event_type     String    @db.VarChar(128)
  aggregate_type String?   @db.VarChar(64)
  aggregate_id   String?   @db.VarChar(30)
  actor_id       String?   @db.VarChar(30)
  payload        Json?
  processed_at   DateTime? @db.Timestamptz(6)

  @@index([aggregate_type, aggregate_id], map: "domain_even_aggrega_29ab8f_idx")
  @@index([event_type], map: "domain_even_event_t_81b11d_idx")
  @@index([processed_at], map: "domain_even_process_2aa0ab_idx")
  @@index([created_at], map: "domain_event_log_created_at_c9145009")
  @@index([id], map: "domain_event_log_id_23da78b4_like")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model evidence_record {
  created_at                DateTime  @db.Timestamptz(6)
  updated_at                DateTime  @db.Timestamptz(6)
  id                        String    @id @db.VarChar(30)
  kind                      String    @db.VarChar(32)
  uri                       String    @db.VarChar(512)
  original_name             String?   @db.VarChar(512)
  mime_type                 String?   @db.VarChar(128)
  file_extension            String?   @db.VarChar(16)
  file_size                 Int?
  storage_provider          String    @db.VarChar(32)
  notes                     String?
  uploaded_by               String    @db.VarChar(30)
  uploader_role             String?   @db.VarChar(32)
  status                    String    @db.VarChar(16)
  reviewed_by               String?   @db.VarChar(30)
  reviewed_at               DateTime? @db.Timestamptz(6)
  review_note               String?   @db.VarChar(512)
  scan_status               String    @db.VarChar(16)
  quarantined               Boolean
  preview_status            String    @db.VarChar(16)
  pdf_rendition_storage_key String?   @db.VarChar(512)
  pdf_rendition_status      String?   @db.VarChar(16)
  pdf_rendition_error       String?   @db.VarChar(512)
  pdf_rendition_at          DateTime? @db.Timestamptz(6)
  view_count                Int
  activity_id               String    @db.VarChar(30)
  activity                  activity  @relation(fields: [activity_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "evidence_record_activity_id_9c02e363_fk_activity_id")

  @@index([activity_id], map: "evidence_re_activit_0e5953_idx")
  @@index([preview_status], map: "evidence_re_preview_70c4e5_idx")
  @@index([quarantined], map: "evidence_re_quarant_cb4f34_idx")
  @@index([activity_id], map: "evidence_record_activity_id_9c02e363")
  @@index([activity_id], map: "evidence_record_activity_id_9c02e363_like")
  @@index([created_at], map: "evidence_record_created_at_75ee5fb8")
  @@index([id], map: "evidence_record_id_8312ba68_like")
}

model fund_request {
  created_at                  DateTime            @db.Timestamptz(6)
  updated_at                  DateTime            @db.Timestamptz(6)
  id                          String              @id @db.VarChar(30)
  fy                          String              @db.VarChar(16)
  period                      String              @db.VarChar(16)
  period_key                  String              @db.VarChar(32)
  scope                       String              @db.VarChar(16)
  submitted_by_user_id        String              @db.VarChar(30)
  submitted_by_role           String              @db.VarChar(64)
  total_amount                BigInt
  activity_count              Int
  status                      String              @db.VarChar(32)
  reviewed_by_user_id         String?             @db.VarChar(30)
  reviewed_at                 DateTime?           @db.Timestamptz(6)
  review_note                 String?             @db.VarChar(512)
  disbursed_amount            BigInt?
  disbursed_at                DateTime?           @db.Timestamptz(6)
  disbursed_by_user_id        String?             @db.VarChar(30)
  disburse_method             String?             @db.VarChar(64)
  disburse_reference          String?             @db.VarChar(128)
  accounted_amount            BigInt?
  returned_amount             BigInt?
  accountability_status       String?             @db.VarChar(32)
  accountability_netsuite_id  String?             @db.VarChar(128)
  accountability_submitted_at DateTime?           @db.Timestamptz(6)
  accountability_reviewed_at  DateTime?           @db.Timestamptz(6)
  fund_request_item           fund_request_item[]

  @@unique([submitted_by_user_id, period, period_key], map: "uniq_request_period_owner")
  @@index([fy, period], map: "fund_reques_fy_245341_idx")
  @@index([status], map: "fund_reques_status_9abf3b_idx")
  @@index([submitted_by_user_id], map: "fund_reques_submitt_b19958_idx")
  @@index([created_at], map: "fund_request_created_at_56ca5ba9")
  @@index([id], map: "fund_request_id_407ceb3c_like")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model fund_request_item {
  created_at                     DateTime     @db.Timestamptz(6)
  updated_at                     DateTime     @db.Timestamptz(6)
  id                             String       @id @db.VarChar(30)
  activity_id                    String       @db.VarChar(30)
  activity_schedule_cost_line_id String       @db.VarChar(30)
  amount                         BigInt
  period                         String       @db.VarChar(16)
  period_key                     String       @db.VarChar(32)
  added_after_generation         Boolean
  fund_request_id                String       @db.VarChar(30)
  fund_request                   fund_request @relation(fields: [fund_request_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fund_request_item_fund_request_id_aaf7f85f_fk_fund_request_id")

  @@unique([activity_schedule_cost_line_id, period, period_key], map: "uniq_costline_period")
  @@unique([fund_request_id, activity_schedule_cost_line_id], map: "uniq_request_costline")
  @@index([activity_id], map: "fund_reques_activit_c76a4a_idx")
  @@index([created_at], map: "fund_request_item_created_at_363e96a3")
  @@index([fund_request_id], map: "fund_request_item_fund_request_id_aaf7f85f")
  @@index([fund_request_id], map: "fund_request_item_fund_request_id_aaf7f85f_like")
  @@index([id], map: "fund_request_item_id_de1f9c7a_like")
}

model geography_alias {
  created_at       DateTime @db.Timestamptz(6)
  updated_at       DateTime @db.Timestamptz(6)
  id               String   @id @db.VarChar(30)
  admin_level      String   @db.VarChar(64)
  admin_id         String   @db.VarChar(30)
  alias            String   @db.VarChar(255)
  normalized_alias String   @db.VarChar(255)
  source           String?  @db.VarChar(255)
  confidence       String?  @db.VarChar(64)

  @@unique([admin_level, normalized_alias], map: "uniq_geoalias_level_norm")
  @@index([admin_level, admin_id], map: "geography_a_admin_l_c988c7_idx")
  @@index([created_at], map: "geography_alias_created_at_ecc93f31")
  @@index([id], map: "geography_alias_id_6ae2f9b1_like")
}

model leadership_decision_insight {
  created_at              DateTime                  @db.Timestamptz(6)
  updated_at              DateTime                  @db.Timestamptz(6)
  id                      String                    @id @db.VarChar(30)
  fy                      String                    @db.VarChar(16)
  quarter                 String?                   @db.VarChar(8)
  decision_type           String                    @db.VarChar(32)
  scope_type              String                    @db.VarChar(16)
  scope_id                String?                   @db.VarChar(30)
  scope_name              String?                   @db.VarChar(255)
  recommendation          String
  reason                  String
  risk_level              String                    @db.VarChar(16)
  confidence_level        String                    @db.VarChar(16)
  confidence_score        Float
  evidence_summary        Json?
  context_adjustment      String?                   @db.VarChar(512)
  financial_implication   String?                   @db.VarChar(512)
  suggested_action        String                    @db.VarChar(512)
  alternatives            Json?
  metrics                 Json?
  risk_flags              String[]                  @db.VarChar(64)
  status                  String                    @db.VarChar(32)
  reviewed_by_user_id     String?                   @db.VarChar(30)
  reviewed_by_role        String?                   @db.VarChar(64)
  reviewed_at             DateTime?                 @db.Timestamptz(6)
  review_note             String?
  generated_at            DateTime                  @db.Timestamptz(6)
  leadership_decisionnote leadership_decisionnote[]

  @@index([confidence_level], map: "leadership__confide_5ab6a8_idx")
  @@index([fy, decision_type], map: "leadership__fy_a25299_idx")
  @@index([risk_level], map: "leadership__risk_le_7cdca3_idx")
  @@index([scope_type, scope_id], map: "leadership__scope_t_2f8a8f_idx")
  @@index([status], map: "leadership__status_4c8126_idx")
  @@index([created_at], map: "leadership_decision_insight_created_at_c132a1f1")
  @@index([id], map: "leadership_decision_insight_id_20688523_like")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model leadership_decisionnote {
  created_at                  DateTime                    @db.Timestamptz(6)
  updated_at                  DateTime                    @db.Timestamptz(6)
  id                          String                      @id @db.VarChar(30)
  author_user_id              String                      @db.VarChar(30)
  author_role                 String                      @db.VarChar(64)
  note                        String
  kind                        String                      @db.VarChar(32)
  insight_id                  String                      @db.VarChar(30)
  leadership_decision_insight leadership_decision_insight @relation(fields: [insight_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "leadership_decisionn_insight_id_ffd149d5_fk_leadershi")

  @@index([created_at], map: "leadership_decisionnote_created_at_3489fba1")
  @@index([id], map: "leadership_decisionnote_id_9360644e_like")
  @@index([insight_id], map: "leadership_decisionnote_insight_id_ffd149d5")
  @@index([insight_id], map: "leadership_decisionnote_insight_id_ffd149d5_like")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model leave {
  created_at          DateTime      @db.Timestamptz(6)
  updated_at          DateTime      @db.Timestamptz(6)
  id                  String        @id @db.VarChar(30)
  type                String        @db.VarChar(64)
  start_date          String        @db.VarChar(32)
  end_date            String        @db.VarChar(32)
  days                Int
  status              String        @db.VarChar(32)
  reason              String?
  reviewed_by_user_id String?       @db.VarChar(30)
  reviewed_at         DateTime?     @db.Timestamptz(6)
  staff_id            String        @db.VarChar(30)
  staff_profile       staff_profile @relation(fields: [staff_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "leave_staff_id_0f1b6c58_fk_staff_profile_id")

  @@index([created_at], map: "leave_created_at_85919f65")
  @@index([id], map: "leave_id_f8c7c680_like")
  @@index([staff_id], map: "leave_staff_i_3b0697_idx")
  @@index([staff_id], map: "leave_staff_id_0f1b6c58")
  @@index([staff_id], map: "leave_staff_id_0f1b6c58_like")
  @@index([status], map: "leave_status_69e783_idx")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model message {
  created_at              DateTime                @db.Timestamptz(6)
  updated_at              DateTime                @db.Timestamptz(6)
  id                      String                  @id @db.VarChar(30)
  sender_id               String                  @db.VarChar(30)
  recipient_id            String?                 @db.VarChar(30)
  body                    String
  category                String?                 @db.VarChar(64)
  context_type            String?                 @db.VarChar(64)
  context_id              String?                 @db.VarChar(30)
  target_route            String?                 @db.VarChar(255)
  priority                String                  @db.VarChar(16)
  action_required         Boolean
  status                  String                  @db.VarChar(16)
  thread_id               String                  @db.VarChar(30)
  messaging_messagethread messaging_messagethread @relation(fields: [thread_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "message_thread_id_8f95b9c8_fk_messaging_messagethread_id")

  @@index([created_at], map: "message_created_at_1730c33f")
  @@index([id], map: "message_id_32d2b4a4_like")
  @@index([recipient_id, status], map: "message_recipie_a2b676_idx")
  @@index([thread_id], map: "message_thread_id_8f95b9c8")
  @@index([thread_id], map: "message_thread_id_8f95b9c8_like")
}

model messaging_messagethread {
  created_at   DateTime  @db.Timestamptz(6)
  updated_at   DateTime  @db.Timestamptz(6)
  id           String    @id @db.VarChar(30)
  subject      String    @db.VarChar(255)
  context_type String?   @db.VarChar(64)
  context_id   String?   @db.VarChar(30)
  message      message[]

  @@index([created_at], map: "messaging_messagethread_created_at_6e3c6507")
  @@index([id], map: "messaging_messagethread_id_e87b257d_like")
}

model monthly_fund_request {
  created_at DateTime @db.Timestamptz(6)
  updated_at DateTime @db.Timestamptz(6)
  id         String   @id @db.VarChar(30)
  fy         String   @db.VarChar(16)
  month      Int
  staff_id   String?  @db.VarChar(30)
  amount     BigInt
  status     String   @db.VarChar(32)

  @@index([created_at], map: "monthly_fund_request_created_at_4ac0945e")
  @@index([id], map: "monthly_fund_request_id_2b5b1727_like")
}

model monthly_plan {
  created_at            DateTime                @db.Timestamptz(6)
  updated_at            DateTime                @db.Timestamptz(6)
  id                    String                  @id @db.VarChar(30)
  month_iso             String                  @db.VarChar(16)
  owner_staff_id        String                  @db.VarChar(30)
  owner_name            String?                 @db.VarChar(255)
  country_id            String                  @db.VarChar(64)
  status                String                  @db.VarChar(32)
  submitted_at          DateTime?               @db.Timestamptz(6)
  approved_at           DateTime?               @db.Timestamptz(6)
  approved_by_id        String?                 @db.VarChar(30)
  returned_reason       String?                 @db.VarChar(512)
  total_cost_cents      Int
  monthly_plan_activity monthly_plan_activity[]

  @@unique([month_iso, owner_staff_id], map: "uniq_monthlyplan_month_owner")
  @@index([owner_staff_id], map: "monthly_pla_owner_s_d6d62c_idx")
  @@index([status], map: "monthly_pla_status_34ed38_idx")
  @@index([created_at], map: "monthly_plan_created_at_1682a31c")
  @@index([id], map: "monthly_plan_id_400d0915_like")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model monthly_plan_activity {
  created_at        DateTime     @db.Timestamptz(6)
  updated_at        DateTime     @db.Timestamptz(6)
  id                String       @id @db.VarChar(30)
  kind              String       @db.VarChar(48)
  title             String       @db.VarChar(255)
  week_of_month     Int
  scheduled_date    String?      @db.VarChar(32)
  school_id         String?      @db.VarChar(30)
  assignee_id       String?      @db.VarChar(30)
  est_cost_cents    Int
  status            String       @db.VarChar(32)
  intervention_area String?      @db.VarChar(64)
  delivery_type     String?      @db.VarChar(16)
  partner_name      String?      @db.VarChar(255)
  plan_id           String       @db.VarChar(30)
  monthly_plan      monthly_plan @relation(fields: [plan_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "monthly_plan_activity_plan_id_5c5e2055_fk_monthly_plan_id")

  @@index([plan_id], map: "monthly_pla_plan_id_114a25_idx")
  @@index([created_at], map: "monthly_plan_activity_created_at_f515d5ed")
  @@index([id], map: "monthly_plan_activity_id_2a51db27_like")
  @@index([plan_id], map: "monthly_plan_activity_plan_id_5c5e2055")
  @@index([plan_id], map: "monthly_plan_activity_plan_id_5c5e2055_like")
}

model monthly_work_plan_budget {
  created_at              DateTime            @db.Timestamptz(6)
  updated_at              DateTime            @db.Timestamptz(6)
  id                      String              @id @db.VarChar(30)
  fy                      String              @db.VarChar(16)
  month_key               String              @db.VarChar(16)
  country_id              String?             @db.VarChar(64)
  generated_at            DateTime            @db.Timestamptz(6)
  generated_by            String?             @db.VarChar(30)
  status                  String              @db.VarChar(32)
  program_total           BigInt
  admin_total             BigInt
  total_amount            BigInt
  activity_count          Int
  submitted_at            DateTime?           @db.Timestamptz(6)
  submitted_by_user_id    String?             @db.VarChar(30)
  rvp_reviewed_at         DateTime?           @db.Timestamptz(6)
  rvp_reviewed_by_user_id String?             @db.VarChar(30)
  rvp_review_note         String?             @db.VarChar(512)
  sent_to_accountant_at   DateTime?           @db.Timestamptz(6)
  admin_budget_line       admin_budget_line[]

  @@unique([country_id, month_key], map: "uniq_country_month")
  @@index([fy, month_key], map: "monthly_wor_fy_2f2e78_idx")
  @@index([status], map: "monthly_wor_status_a0db5d_idx")
  @@index([created_at], map: "monthly_work_plan_budget_created_at_6dbf14ee")
  @@index([id], map: "monthly_work_plan_budget_id_6aafa194_like")
}

model notification {
  created_at        DateTime  @db.Timestamptz(6)
  updated_at        DateTime  @db.Timestamptz(6)
  id                String    @id @db.VarChar(30)
  recipient_id      String    @db.VarChar(30)
  recipient_role    String?   @db.VarChar(64)
  title             String    @db.VarChar(255)
  body              String?
  context_type      String?   @db.VarChar(64)
  context_id        String?   @db.VarChar(30)
  target_route      String?   @db.VarChar(255)
  action_label      String?   @db.VarChar(64)
  action_required   Boolean
  priority          String    @db.VarChar(16)
  status            String    @db.VarChar(16)
  source_event_type String?   @db.VarChar(64)
  source_event_id   String?   @db.VarChar(30)
  expires_at        DateTime? @db.Timestamptz(6)
  read_at           DateTime? @db.Timestamptz(6)

  @@index([recipient_id, status], map: "notificatio_recipie_d40309_idx")
  @@index([source_event_id], map: "notificatio_source__f5a4b0_idx")
  @@index([created_at], map: "notification_created_at_02eea978")
  @@index([id], map: "notification_id_2ec17b22_like")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model parish {
  created_at    DateTime   @db.Timestamptz(6)
  updated_at    DateTime   @db.Timestamptz(6)
  id            String     @id @db.VarChar(30)
  name          String     @db.VarChar(255)
  pcode         String?    @unique @db.VarChar(64)
  source        String?    @db.VarChar(255)
  confidence    String?    @db.VarChar(64)
  sub_county_id String     @db.VarChar(30)
  sub_county    sub_county @relation(fields: [sub_county_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "parish_sub_county_id_48505d73_fk_sub_county_id")
  school        school[]
  village       village[]

  @@unique([sub_county_id, name], map: "uniq_parish_subcounty_name")
  @@index([created_at], map: "parish_created_at_0c605696")
  @@index([id], map: "parish_id_58ba3c1b_like")
  @@index([pcode], map: "parish_pcode_db7c9acd_like")
  @@index([sub_county_id], map: "parish_sub_county_id_48505d73")
  @@index([sub_county_id], map: "parish_sub_county_id_48505d73_like")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model partner {
  created_at                 DateTime                     @db.Timestamptz(6)
  updated_at                 DateTime                     @db.Timestamptz(6)
  deleted_at                 DateTime?                    @db.Timestamptz(6)
  id                         String                       @id @db.VarChar(30)
  name                       String                       @db.VarChar(255)
  region_name                String?                      @db.VarChar(255)
  trains_on                  String[]                     @db.VarChar(128)
  notes                      String?
  contact_person             String?                      @db.VarChar(255)
  email                      String?                      @db.VarChar(254)
  phone                      String?                      @db.VarChar(64)
  coverage_districts         String[]                     @db.VarChar(255)
  contract_status            String?                      @db.VarChar(32)
  onboarded_by_user_id       String?                      @db.VarChar(30)
  onboarded_at               DateTime?                    @db.Timestamptz(6)
  is_certified               Boolean
  certification_status       String?                      @db.VarChar(32)
  expertise_areas            String[]                     @db.VarChar(128)
  active_status              Boolean
  user_id                    String?                      @unique @db.VarChar(30)
  environment                String                       @db.VarChar(16)
  source                     String                       @db.VarChar(32)
  user                       user?                        @relation(fields: [user_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "partner_user_id_c28ba7ed_fk_user_id")
  project_partner_assignment project_partner_assignment[]

  @@index([created_at], map: "partner_created_at_8338c187")
  @@index([deleted_at], map: "partner_deleted_at_1b644f7f")
  @@index([environment], map: "partner_environment_ac7157ae")
  @@index([environment], map: "partner_environment_ac7157ae_like")
  @@index([id], map: "partner_id_3ed17e2a_like")
  @@index([source], map: "partner_source_42a4f18b")
  @@index([source], map: "partner_source_42a4f18b_like")
  @@index([user_id], map: "partner_user_id_c28ba7ed_like")
}

model permission {
  created_at      DateTime          @db.Timestamptz(6)
  updated_at      DateTime          @db.Timestamptz(6)
  id              String            @id @db.VarChar(30)
  key             String            @unique @db.VarChar(128)
  description     String?
  role_permission role_permission[]

  @@index([created_at], map: "permission_created_at_d069ca62")
  @@index([id], map: "permission_id_cfdf17ce_like")
  @@index([key], map: "permission_key_45426191_like")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model planning_annualplanactivity {
  created_at     DateTime    @db.Timestamptz(6)
  updated_at     DateTime    @db.Timestamptz(6)
  id             String      @id @db.VarChar(30)
  activity_type  String      @db.VarChar(48)
  school_id      String?     @db.VarChar(30)
  cluster_id     String?     @db.VarChar(30)
  quarter        String      @db.VarChar(8)
  month          Int?
  week           Int?
  annual_plan_id String      @db.VarChar(30)
  annual_plan    annual_plan @relation(fields: [annual_plan_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "planning_annualplana_annual_plan_id_0bbf8736_fk_annual_pl")

  @@index([annual_plan_id], map: "planning_annualplanactivity_annual_plan_id_0bbf8736")
  @@index([annual_plan_id], map: "planning_annualplanactivity_annual_plan_id_0bbf8736_like")
  @@index([created_at], map: "planning_annualplanactivity_created_at_a71b0495")
  @@index([id], map: "planning_annualplanactivity_id_d35c813a_like")
}

model project {
  created_at                 DateTime                     @db.Timestamptz(6)
  updated_at                 DateTime                     @db.Timestamptz(6)
  deleted_at                 DateTime?                    @db.Timestamptz(6)
  id                         String                       @id @db.VarChar(30)
  code                       String?                      @unique @db.VarChar(64)
  name                       String                       @db.VarChar(255)
  category                   String                       @db.VarChar(32)
  intervention               String?                      @db.VarChar(64)
  manager_staff_id           String?                      @db.VarChar(30)
  environment                String                       @db.VarChar(16)
  source                     String                       @db.VarChar(32)
  project_impact_snapshot    project_impact_snapshot[]
  project_partner_assignment project_partner_assignment[]
  project_school_assignment  project_school_assignment[]

  @@index([code], map: "project_code_61e6b1c9_like")
  @@index([created_at], map: "project_created_at_6ba07933")
  @@index([deleted_at], map: "project_deleted_at_5130dcb9")
  @@index([environment], map: "project_environment_7923c844")
  @@index([environment], map: "project_environment_7923c844_like")
  @@index([id], map: "project_id_912855ca_like")
  @@index([source], map: "project_source_7a004cee")
  @@index([source], map: "project_source_7a004cee_like")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model project_impact_snapshot {
  created_at   DateTime @db.Timestamptz(6)
  updated_at   DateTime @db.Timestamptz(6)
  id           String   @id @db.VarChar(30)
  fy           String   @db.VarChar(16)
  metrics_json Json
  project_id   String   @db.VarChar(30)
  project      project  @relation(fields: [project_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "project_impact_snapshot_project_id_acc58143_fk_project_id")

  @@index([created_at], map: "project_impact_snapshot_created_at_cf571c1f")
  @@index([id], map: "project_impact_snapshot_id_cd4207f2_like")
  @@index([project_id], map: "project_impact_snapshot_project_id_acc58143")
  @@index([project_id], map: "project_impact_snapshot_project_id_acc58143_like")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model project_partner_assignment {
  created_at DateTime @db.Timestamptz(6)
  updated_at DateTime @db.Timestamptz(6)
  id         String   @id @db.VarChar(30)
  partner_id String   @db.VarChar(30)
  project_id String   @db.VarChar(30)
  partner    partner  @relation(fields: [partner_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "project_partner_assignment_partner_id_188edd1d_fk_partner_id")
  project    project  @relation(fields: [project_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "project_partner_assignment_project_id_dc97f814_fk_project_id")

  @@unique([project_id, partner_id], map: "uniq_project_partner")
  @@index([created_at], map: "project_partner_assignment_created_at_4e11e886")
  @@index([id], map: "project_partner_assignment_id_ff98ae5e_like")
  @@index([partner_id], map: "project_partner_assignment_partner_id_188edd1d")
  @@index([partner_id], map: "project_partner_assignment_partner_id_188edd1d_like")
  @@index([project_id], map: "project_partner_assignment_project_id_dc97f814")
  @@index([project_id], map: "project_partner_assignment_project_id_dc97f814_like")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model project_school_assignment {
  created_at DateTime @db.Timestamptz(6)
  updated_at DateTime @db.Timestamptz(6)
  id         String   @id @db.VarChar(30)
  project_id String   @db.VarChar(30)
  school_id  String   @db.VarChar(30)
  project    project  @relation(fields: [project_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "project_school_assignment_project_id_014a6c36_fk_project_id")
  school     school   @relation(fields: [school_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "project_school_assignment_school_id_ea0fedca_fk_school_id")

  @@unique([project_id, school_id], map: "uniq_project_school")
  @@index([created_at], map: "project_school_assignment_created_at_30bf5562")
  @@index([id], map: "project_school_assignment_id_d2fdb888_like")
  @@index([project_id], map: "project_school_assignment_project_id_014a6c36")
  @@index([project_id], map: "project_school_assignment_project_id_014a6c36_like")
  @@index([school_id], map: "project_school_assignment_school_id_ea0fedca")
  @@index([school_id], map: "project_school_assignment_school_id_ea0fedca_like")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model refresh_token {
  created_at DateTime  @db.Timestamptz(6)
  updated_at DateTime  @db.Timestamptz(6)
  id         String    @id @db.VarChar(30)
  token_hash String    @unique @db.VarChar(255)
  expires_at DateTime  @db.Timestamptz(6)
  revoked_at DateTime? @db.Timestamptz(6)
  user_id    String    @db.VarChar(30)
  user       user      @relation(fields: [user_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "refresh_token_user_id_1d7a63ac_fk_user_id")

  @@index([user_id], map: "refresh_tok_user_id_79bd41_idx")
  @@index([created_at], map: "refresh_token_created_at_75945b3a")
  @@index([id], map: "refresh_token_id_32362ac1_like")
  @@index([token_hash], map: "refresh_token_token_hash_3977344a_like")
  @@index([user_id], map: "refresh_token_user_id_1d7a63ac")
  @@index([user_id], map: "refresh_token_user_id_1d7a63ac_like")
}

model region {
  created_at DateTime     @db.Timestamptz(6)
  updated_at DateTime     @db.Timestamptz(6)
  id         String       @id @db.VarChar(30)
  name       String       @unique @db.VarChar(255)
  code       String?      @unique @db.VarChar(64)
  pcode      String?      @unique @db.VarChar(64)
  source     String?      @db.VarChar(255)
  latitude   Float?
  longitude  Float?
  cluster    cluster[]
  district   district[]
  school     school[]
  sub_region sub_region[]

  @@index([code], map: "region_code_a2414646_like")
  @@index([created_at], map: "region_created_at_9c1e695a")
  @@index([id], map: "region_id_884aa0d8_like")
  @@index([name], map: "region_name_5fc567df_like")
  @@index([pcode], map: "region_pcode_01ab3ba9_like")
}

model report {
  created_at         DateTime @db.Timestamptz(6)
  updated_at         DateTime @db.Timestamptz(6)
  id                 String   @id @db.VarChar(30)
  title              String   @db.VarChar(255)
  type               String   @db.VarChar(64)
  fy                 String   @db.VarChar(16)
  scope              String   @db.VarChar(64)
  created_by_user_id String?  @db.VarChar(30)
  summary_json       Json

  @@index([created_at], map: "report_created_at_cc48a21a")
  @@index([fy], map: "report_fy_a26323_idx")
  @@index([id], map: "report_id_0debcac0_like")
  @@index([type], map: "report_type_9eba38_idx")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model role_permission {
  created_at    DateTime   @db.Timestamptz(6)
  updated_at    DateTime   @db.Timestamptz(6)
  id            String     @id @db.VarChar(30)
  role          String     @db.VarChar(64)
  permission_id String     @db.VarChar(30)
  permission    permission @relation(fields: [permission_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "role_permission_permission_id_ee9c5982_fk_permission_id")

  @@unique([role, permission_id], map: "uniq_role_permission")
  @@index([created_at], map: "role_permission_created_at_dff82943")
  @@index([id], map: "role_permission_id_be89bc7c_like")
  @@index([permission_id], map: "role_permission_permission_id_ee9c5982")
  @@index([permission_id], map: "role_permission_permission_id_ee9c5982_like")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model school {
  created_at                                                                 DateTime                     @db.Timestamptz(6)
  updated_at                                                                 DateTime                     @db.Timestamptz(6)
  deleted_at                                                                 DateTime?                    @db.Timestamptz(6)
  id                                                                         String                       @id @db.VarChar(30)
  school_id                                                                  String                       @unique @db.VarChar(64)
  name                                                                       String                       @db.VarChar(512)
  sub_region_id                                                              String?                      @db.VarChar(30)
  county_id                                                                  String?                      @db.VarChar(30)
  latitude                                                                   Float?
  longitude                                                                  Float?
  uploaded_region_text                                                       String?                      @db.VarChar(255)
  uploaded_district_text                                                     String?                      @db.VarChar(255)
  uploaded_sub_county_text                                                   String?                      @db.VarChar(255)
  uploaded_parish_text                                                       String?                      @db.VarChar(255)
  geography_match_status                                                     String?                      @db.VarChar(64)
  geography_match_confidence                                                 Float?
  geography_match_warnings                                                   Json?
  shipping_address                                                           String?                      @db.VarChar(512)
  school_phone                                                               String?                      @db.VarChar(64)
  primary_contact_name                                                       String?                      @db.VarChar(255)
  primary_contact_phone                                                      String?                      @db.VarChar(64)
  enrollment                                                                 Int?
  school_type                                                                String                       @db.VarChar(32)
  account_owner_id                                                           String?                      @db.VarChar(30)
  account_owner_name_raw                                                     String?                      @db.VarChar(255)
  account_owner_status                                                       String                       @db.VarChar(32)
  duplicate_status                                                           String                       @db.VarChar(32)
  cluster_id                                                                 String?                      @db.VarChar(30)
  cluster_status                                                             String                       @db.VarChar(32)
  current_fy_ssa_status                                                      String                       @db.VarChar(32)
  planning_readiness                                                         String                       @db.VarChar(32)
  salesforce_account_id                                                      String?                      @db.VarChar(128)
  salesforce_sync_status                                                     String                       @db.VarChar(32)
  salesforce_last_synced_at                                                  DateTime?                    @db.Timestamptz(6)
  salesforce_sync_error                                                      String?                      @db.VarChar(512)
  created_by_ia                                                              Boolean
  upload_batch_id                                                            String?                      @db.VarChar(30)
  district_id                                                                String                       @db.VarChar(30)
  parish_id                                                                  String?                      @db.VarChar(30)
  region_id                                                                  String                       @db.VarChar(30)
  sub_county_id                                                              String?                      @db.VarChar(30)
  environment                                                                String                       @db.VarChar(16)
  source                                                                     String                       @db.VarChar(32)
  last_enrollment_date                                                       DateTime?                    @db.Date
  activity                                                                   activity[]
  project_school_assignment                                                  project_school_assignment[]
  district                                                                   district                     @relation(fields: [district_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "school_district_id_c05d66f8_fk_district_id")
  parish                                                                     parish?                      @relation(fields: [parish_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "school_parish_id_d04eccc5_fk_parish_id")
  region                                                                     region                       @relation(fields: [region_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "school_region_id_728f6299_fk_region_id")
  sub_county                                                                 sub_county?                  @relation(fields: [sub_county_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "school_sub_county_id_9af6a9e6_fk_sub_county_id")
  school_cluster_assignment                                                  school_cluster_assignment[]
  school_duplicate_candidate_school_duplicate_candidate_candidate_idToschool school_duplicate_candidate[] @relation("school_duplicate_candidate_candidate_idToschool")
  school_duplicate_candidate_school_duplicate_candidate_school_idToschool    school_duplicate_candidate[] @relation("school_duplicate_candidate_school_idToschool")
  school_enrollment_history                                                  school_enrollment_history[]
  ssa_record                                                                 ssa_record[]

  @@index([cluster_id], map: "school_cluster_edaa4b_idx")
  @@index([cluster_status], map: "school_cluster_st_idx")
  @@index([created_at], map: "school_created_at_144a4320")
  @@index([deleted_at], map: "school_deleted_at_3a60b3cf")
  @@index([district_id], map: "school_distric_656d13_idx")
  @@index([district_id], map: "school_district_id_c05d66f8")
  @@index([district_id], map: "school_district_id_c05d66f8_like")
  @@index([duplicate_status], map: "school_dup_status_idx")
  @@index([environment], map: "school_environment_f3ef5675")
  @@index([environment], map: "school_environment_f3ef5675_like")
  @@index([id], map: "school_id_965e14aa_like")
  @@index([account_owner_status], map: "school_owner_st_idx")
  @@index([parish_id], map: "school_parish_id_d04eccc5")
  @@index([parish_id], map: "school_parish_id_d04eccc5_like")
  @@index([planning_readiness], map: "school_plan_ready_idx")
  @@index([region_id], map: "school_region__236ce4_idx")
  @@index([region_id], map: "school_region_id_728f6299")
  @@index([region_id], map: "school_region_id_728f6299_like")
  @@index([school_type], map: "school_school__12f8fd_idx")
  @@index([school_id], map: "school_school_id_240f7bc7_like")
  @@index([source], map: "school_source_7f658628")
  @@index([source], map: "school_source_7f658628_like")
  @@index([current_fy_ssa_status], map: "school_ssa_status_idx")
  @@index([sub_county_id], map: "school_sub_county_id_9af6a9e6")
  @@index([sub_county_id], map: "school_sub_county_id_9af6a9e6_like")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model school_account_owner_upload_map {
  created_at       DateTime     @db.Timestamptz(6)
  updated_at       DateTime     @db.Timestamptz(6)
  id               String       @id @db.VarChar(30)
  school_id_raw    String       @db.VarChar(128)
  owner_name_raw   String       @db.VarChar(255)
  matched_staff_id String?      @db.VarChar(30)
  matched          Boolean
  upload_batch_id  String       @db.VarChar(30)
  upload_batch     upload_batch @relation(fields: [upload_batch_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "school_account_owner_upload_batch_id_a367c35b_fk_upload_ba")

  @@index([created_at], map: "school_account_owner_upload_map_created_at_a8c632bb")
  @@index([id], map: "school_account_owner_upload_map_id_41f66f32_like")
  @@index([upload_batch_id], map: "school_account_owner_upload_map_upload_batch_id_a367c35b")
  @@index([upload_batch_id], map: "school_account_owner_upload_map_upload_batch_id_a367c35b_like")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model school_cluster_assignment {
  created_at  DateTime @db.Timestamptz(6)
  updated_at  DateTime @db.Timestamptz(6)
  id          String   @id @db.VarChar(30)
  assigned_by String   @db.VarChar(30)
  cluster_id  String   @db.VarChar(30)
  school_id   String   @db.VarChar(30)
  cluster     cluster  @relation(fields: [cluster_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "school_cluster_assignment_cluster_id_1b2315be_fk_cluster_id")
  school      school   @relation(fields: [school_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "school_cluster_assignment_school_id_1569af41_fk_school_id")

  @@unique([school_id, cluster_id], map: "uniq_school_cluster")
  @@index([cluster_id], map: "school_cluster_assignment_cluster_id_1b2315be")
  @@index([cluster_id], map: "school_cluster_assignment_cluster_id_1b2315be_like")
  @@index([created_at], map: "school_cluster_assignment_created_at_d20e9ce5")
  @@index([id], map: "school_cluster_assignment_id_97263086_like")
  @@index([school_id], map: "school_cluster_assignment_school_id_1569af41")
  @@index([school_id], map: "school_cluster_assignment_school_id_1569af41_like")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model school_duplicate_candidate {
  created_at                                             DateTime @db.Timestamptz(6)
  updated_at                                             DateTime @db.Timestamptz(6)
  id                                                     String   @id @db.VarChar(30)
  score                                                  Int
  reasons                                                String[] @db.VarChar(64)
  resolved                                               Boolean
  resolution                                             String?  @db.VarChar(32)
  candidate_id                                           String   @db.VarChar(30)
  school_id                                              String   @db.VarChar(30)
  school_school_duplicate_candidate_candidate_idToschool school   @relation("school_duplicate_candidate_candidate_idToschool", fields: [candidate_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "school_duplicate_candidate_candidate_id_c50f3d0b_fk_school_id")
  school_school_duplicate_candidate_school_idToschool    school   @relation("school_duplicate_candidate_school_idToschool", fields: [school_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "school_duplicate_candidate_school_id_22e4f18a_fk_school_id")

  @@unique([school_id, candidate_id], map: "uniq_dup_school_candidate")
  @@index([candidate_id], map: "school_duplicate_candidate_candidate_id_c50f3d0b")
  @@index([candidate_id], map: "school_duplicate_candidate_candidate_id_c50f3d0b_like")
  @@index([created_at], map: "school_duplicate_candidate_created_at_265ce563")
  @@index([id], map: "school_duplicate_candidate_id_dcb55d59_like")
  @@index([school_id], map: "school_duplicate_candidate_school_id_22e4f18a")
  @@index([school_id], map: "school_duplicate_candidate_school_id_22e4f18a_like")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model school_enrollment_history {
  created_at  DateTime @db.Timestamptz(6)
  updated_at  DateTime @db.Timestamptz(6)
  id          String   @id @db.VarChar(30)
  fy          String   @db.VarChar(16)
  enrollment  Int
  recorded_at DateTime @db.Timestamptz(6)
  school_id   String   @db.VarChar(30)
  school      school   @relation(fields: [school_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "school_enrollment_history_school_id_20b83044_fk_school_id")

  @@unique([school_id, fy], map: "uniq_enrollment_school_fy")
  @@index([created_at], map: "school_enrollment_history_created_at_ebc1935e")
  @@index([id], map: "school_enrollment_history_id_25a6dfb4_like")
  @@index([school_id], map: "school_enrollment_history_school_id_20b83044")
  @@index([school_id], map: "school_enrollment_history_school_id_20b83044_like")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model ssa_record {
  created_at              DateTime    @db.Timestamptz(6)
  updated_at              DateTime    @db.Timestamptz(6)
  deleted_at              DateTime?   @db.Timestamptz(6)
  id                      String      @id @db.VarChar(30)
  date_of_ssa             DateTime    @db.Timestamptz(6)
  fy                      String      @db.VarChar(16)
  quarter                 String      @db.VarChar(8)
  new_enrollment          Int?
  average_score           Float?
  salesforce_id           String?     @db.VarChar(128)
  verification_status     String      @db.VarChar(32)
  collector_type          String      @db.VarChar(32)
  verification_source     String?     @db.VarChar(64)
  collected_by_user_id    String?     @db.VarChar(30)
  collected_by_partner_id String?     @db.VarChar(30)
  verified_by_user_id     String?     @db.VarChar(30)
  verified_at             DateTime?   @db.Timestamptz(6)
  qa_reviewed_by_user_id  String?     @db.VarChar(30)
  qa_reviewed_at          DateTime?   @db.Timestamptz(6)
  uploaded_by             String      @db.VarChar(30)
  school_id               String      @db.VarChar(30)
  environment             String      @db.VarChar(16)
  source                  String      @db.VarChar(32)
  school                  school      @relation(fields: [school_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "ssa_record_school_id_45c30279_fk_school_id")
  ssa_score               ssa_score[]

  @@index([collector_type], map: "ssa_record_collect_44e483_idx")
  @@index([created_at], map: "ssa_record_created_at_8fdb3510")
  @@index([deleted_at], map: "ssa_record_deleted_at_88ecfab3")
  @@index([environment], map: "ssa_record_environment_1f467adb")
  @@index([environment], map: "ssa_record_environment_1f467adb_like")
  @@index([fy], map: "ssa_record_fy_8271f2_idx")
  @@index([id], map: "ssa_record_id_d296be77_like")
  @@index([school_id], map: "ssa_record_school__ca2094_idx")
  @@index([school_id], map: "ssa_record_school_id_45c30279")
  @@index([school_id], map: "ssa_record_school_id_45c30279_like")
  @@index([source], map: "ssa_record_source_de8f0bb6")
  @@index([source], map: "ssa_record_source_de8f0bb6_like")
  @@index([verification_status], map: "ssa_record_verific_3ef0d9_idx")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model ssa_score {
  created_at    DateTime   @db.Timestamptz(6)
  updated_at    DateTime   @db.Timestamptz(6)
  id            String     @id @db.VarChar(30)
  intervention  String     @db.VarChar(64)
  score         Float
  ssa_record_id String     @db.VarChar(30)
  ssa_record    ssa_record @relation(fields: [ssa_record_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "ssa_score_ssa_record_id_70f313f8_fk_ssa_record_id")

  @@unique([ssa_record_id, intervention], map: "uniq_ssa_record_intervention")
  @@index([created_at], map: "ssa_score_created_at_e0c905fc")
  @@index([id], map: "ssa_score_id_a03c78f9_like")
  @@index([ssa_record_id], map: "ssa_score_ssa_record_id_70f313f8")
  @@index([ssa_record_id], map: "ssa_score_ssa_record_id_70f313f8_like")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model staff_geography_assignment {
  created_at    DateTime      @db.Timestamptz(6)
  updated_at    DateTime      @db.Timestamptz(6)
  id            String        @id @db.VarChar(30)
  region_id     String?       @db.VarChar(30)
  district_id   String?       @db.VarChar(30)
  staff_id      String        @db.VarChar(30)
  staff_profile staff_profile @relation(fields: [staff_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "staff_geography_assi_staff_id_b8f03520_fk_staff_pro")

  @@index([staff_id], map: "staff_geogr_staff_i_c4118c_idx")
  @@index([created_at], map: "staff_geography_assignment_created_at_d09e8104")
  @@index([id], map: "staff_geography_assignment_id_8e4cbdba_like")
  @@index([staff_id], map: "staff_geography_assignment_staff_id_b8f03520")
  @@index([staff_id], map: "staff_geography_assignment_staff_id_b8f03520_like")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model staff_profile {
  created_at                                                                           DateTime                      @db.Timestamptz(6)
  updated_at                                                                           DateTime                      @db.Timestamptz(6)
  deleted_at                                                                           DateTime?                     @db.Timestamptz(6)
  id                                                                                   String                        @id @db.VarChar(30)
  staff_number                                                                         String?                       @unique @db.VarChar(64)
  title                                                                                String?                       @db.VarChar(255)
  primary_district_id                                                                  String?                       @db.VarChar(30)
  onboarding_state                                                                     String                        @db.VarChar(32)
  user_id                                                                              String                        @unique @db.VarChar(30)
  environment                                                                          String                        @db.VarChar(16)
  source                                                                               String                        @db.VarChar(32)
  leave                                                                                leave[]
  staff_geography_assignment                                                           staff_geography_assignment[]
  user                                                                                 user                          @relation(fields: [user_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "staff_profile_user_id_9ce05585_fk_user_id")
  staff_school_assignment                                                              staff_school_assignment[]
  staff_supervisor_assignment_staff_supervisor_assignment_supervisee_idTostaff_profile staff_supervisor_assignment[] @relation("staff_supervisor_assignment_supervisee_idTostaff_profile")
  staff_supervisor_assignment_staff_supervisor_assignment_supervisor_idTostaff_profile staff_supervisor_assignment[] @relation("staff_supervisor_assignment_supervisor_idTostaff_profile")
  staff_support_capacity                                                               staff_support_capacity[]
  staff_target_profile                                                                 staff_target_profile[]

  @@index([created_at], map: "staff_profile_created_at_b3170753")
  @@index([deleted_at], map: "staff_profile_deleted_at_37a84155")
  @@index([environment], map: "staff_profile_environment_c22caddf")
  @@index([environment], map: "staff_profile_environment_c22caddf_like")
  @@index([id], map: "staff_profile_id_ce246a0e_like")
  @@index([source], map: "staff_profile_source_6b766693")
  @@index([source], map: "staff_profile_source_6b766693_like")
  @@index([staff_number], map: "staff_profile_staff_number_f4b9197e_like")
  @@index([user_id], map: "staff_profile_user_id_9ce05585_like")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model staff_school_assignment {
  created_at    DateTime      @db.Timestamptz(6)
  updated_at    DateTime      @db.Timestamptz(6)
  id            String        @id @db.VarChar(30)
  school_id     String        @db.VarChar(30)
  staff_id      String        @db.VarChar(30)
  staff_profile staff_profile @relation(fields: [staff_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "staff_school_assignment_staff_id_5b0f0e89_fk_staff_profile_id")

  @@unique([staff_id, school_id], map: "uniq_staff_school")
  @@index([created_at], map: "staff_school_assignment_created_at_3f5a9952")
  @@index([id], map: "staff_school_assignment_id_3e304fc4_like")
  @@index([staff_id], map: "staff_school_assignment_staff_id_5b0f0e89")
  @@index([staff_id], map: "staff_school_assignment_staff_id_5b0f0e89_like")
}

model staff_setup_candidate {
  created_at          DateTime @db.Timestamptz(6)
  updated_at          DateTime @db.Timestamptz(6)
  id                  String   @id @db.VarChar(30)
  full_name           String   @db.VarChar(255)
  normalized_name     String   @unique @db.VarChar(255)
  source_upload_batch String?  @db.VarChar(30)
  school_count        Int
  sample_school_ids   String[] @db.VarChar(30)
  suggested_role      String?  @db.VarChar(32)
  email               String?  @db.VarChar(254)
  phone               String?  @db.VarChar(64)
  status              String   @db.VarChar(32)
  matched_user_id     String?  @db.VarChar(30)

  @@index([created_at], map: "staff_setup_candidate_created_at_b69b4b92")
  @@index([id], map: "staff_setup_candidate_id_d35179ea_like")
  @@index([normalized_name], map: "staff_setup_candidate_normalized_name_94e36a8f_like")
  @@index([normalized_name], map: "staff_setup_normali_72fe93_idx")
  @@index([status], map: "staff_setup_status_11dfda_idx")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model staff_supervisor_assignment {
  created_at                                                             DateTime      @db.Timestamptz(6)
  updated_at                                                             DateTime      @db.Timestamptz(6)
  id                                                                     String        @id @db.VarChar(30)
  supervisee_id                                                          String        @db.VarChar(30)
  supervisor_id                                                          String        @db.VarChar(30)
  staff_profile_staff_supervisor_assignment_supervisee_idTostaff_profile staff_profile @relation("staff_supervisor_assignment_supervisee_idTostaff_profile", fields: [supervisee_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "staff_supervisor_ass_supervisee_id_d849b17b_fk_staff_pro")
  staff_profile_staff_supervisor_assignment_supervisor_idTostaff_profile staff_profile @relation("staff_supervisor_assignment_supervisor_idTostaff_profile", fields: [supervisor_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "staff_supervisor_ass_supervisor_id_b978d4fd_fk_staff_pro")

  @@unique([supervisee_id, supervisor_id], map: "uniq_supervisee_supervisor")
  @@index([supervisor_id], map: "staff_super_supervi_8b1052_idx")
  @@index([created_at], map: "staff_supervisor_assignment_created_at_85215d1d")
  @@index([id], map: "staff_supervisor_assignment_id_4db3d24f_like")
  @@index([supervisee_id], map: "staff_supervisor_assignment_supervisee_id_d849b17b")
  @@index([supervisee_id], map: "staff_supervisor_assignment_supervisee_id_d849b17b_like")
  @@index([supervisor_id], map: "staff_supervisor_assignment_supervisor_id_b978d4fd")
  @@index([supervisor_id], map: "staff_supervisor_assignment_supervisor_id_b978d4fd_like")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model staff_support_capacity {
  created_at                   DateTime      @db.Timestamptz(6)
  updated_at                   DateTime      @db.Timestamptz(6)
  id                           String        @id @db.VarChar(30)
  fy                           String        @db.VarChar(16)
  max_direct_schools_supported Int
  set_by_user_id               String        @db.VarChar(30)
  set_by_role                  String        @db.VarChar(64)
  effective_from               DateTime?     @db.Timestamptz(6)
  effective_to                 DateTime?     @db.Timestamptz(6)
  notes                        String?
  is_active                    Boolean
  staff_id                     String        @db.VarChar(30)
  staff_profile                staff_profile @relation(fields: [staff_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "staff_support_capacity_staff_id_e4e044f5_fk_staff_profile_id")

  @@unique([staff_id, fy], map: "uniq_staff_capacity_fy")
  @@index([fy], map: "staff_suppo_fy_421ac5_idx")
  @@index([created_at], map: "staff_support_capacity_created_at_f12b67a7")
  @@index([id], map: "staff_support_capacity_id_19950b83_like")
  @@index([staff_id], map: "staff_support_capacity_staff_id_e4e044f5")
  @@index([staff_id], map: "staff_support_capacity_staff_id_e4e044f5_like")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model staff_target_profile {
  created_at              DateTime      @db.Timestamptz(6)
  updated_at              DateTime      @db.Timestamptz(6)
  id                      String        @id @db.VarChar(30)
  fy                      String        @db.VarChar(16)
  visits_target           Int
  trainings_target        Int
  staff_id                String        @db.VarChar(30)
  accountability_target   Int
  activity_codes_target   Int
  cluster_meetings_target Int
  evidence_target         Int
  group_trainings_target  Int
  ia_verified_target      Int
  ssa_target              Int
  staff_profile           staff_profile @relation(fields: [staff_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "staff_target_profile_staff_id_066165ba_fk_staff_profile_id")

  @@unique([staff_id, fy], map: "uniq_staff_target_fy")
  @@index([created_at], map: "staff_target_profile_created_at_a751c09c")
  @@index([id], map: "staff_target_profile_id_cee162f4_like")
  @@index([staff_id], map: "staff_target_profile_staff_id_066165ba")
  @@index([staff_id], map: "staff_target_profile_staff_id_066165ba_like")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model sub_county {
  created_at         DateTime             @db.Timestamptz(6)
  updated_at         DateTime             @db.Timestamptz(6)
  id                 String               @id @db.VarChar(30)
  name               String               @db.VarChar(255)
  seeded             Boolean
  pcode              String?              @unique @db.VarChar(64)
  source             String?              @db.VarChar(255)
  latitude           Float?
  longitude          Float?
  county_id          String?              @db.VarChar(30)
  district_id        String               @db.VarChar(30)
  cluster            cluster[]
  cluster_sub_county cluster_sub_county[]
  parish             parish[]
  school             school[]
  county             county?              @relation(fields: [county_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "sub_county_county_id_ec4a76dc_fk_county_id")
  district           district             @relation(fields: [district_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "sub_county_district_id_d9d5c625_fk_district_id")

  @@unique([district_id, name], map: "uniq_subcounty_district_name")
  @@index([county_id], map: "sub_county_county_id_ec4a76dc")
  @@index([county_id], map: "sub_county_county_id_ec4a76dc_like")
  @@index([created_at], map: "sub_county_created_at_c08207a8")
  @@index([district_id], map: "sub_county_district_id_d9d5c625")
  @@index([district_id], map: "sub_county_district_id_d9d5c625_like")
  @@index([id], map: "sub_county_id_0949452d_like")
  @@index([pcode], map: "sub_county_pcode_60f72e04_like")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model sub_region {
  created_at      DateTime   @db.Timestamptz(6)
  updated_at      DateTime   @db.Timestamptz(6)
  id              String     @id @db.VarChar(30)
  name            String     @unique @db.VarChar(255)
  normalized_name String     @db.VarChar(255)
  source          String     @db.VarChar(64)
  confidence      String     @db.VarChar(64)
  verified_by     String?    @db.VarChar(255)
  verified_at     DateTime?  @db.Timestamptz(6)
  notes           String?
  region_id       String     @db.VarChar(30)
  district        district[]
  region          region     @relation(fields: [region_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "sub_region_region_id_27bc4a83_fk_region_id")

  @@index([created_at], map: "sub_region_created_at_ba3b5ea0")
  @@index([id], map: "sub_region_id_a9e47950_like")
  @@index([name], map: "sub_region_name_10e888db_like")
  @@index([region_id], map: "sub_region_region_id_27bc4a83")
  @@index([region_id], map: "sub_region_region_id_27bc4a83_like")
}

model target_setting {
  created_at           DateTime  @db.Timestamptz(6)
  updated_at           DateTime  @db.Timestamptz(6)
  id                   String    @id @db.VarChar(30)
  fy                   String    @db.VarChar(16)
  target_type          String    @db.VarChar(32)
  scope_type           String    @db.VarChar(16)
  scope_id             String?   @db.VarChar(30)
  target_value         Float?
  target_unit          String    @db.VarChar(16)
  target_percentage    Float?
  quarter_distribution Json?
  set_by_user_id       String    @db.VarChar(30)
  set_by_role          String    @db.VarChar(64)
  effective_from       DateTime  @db.Timestamptz(6)
  effective_to         DateTime? @db.Timestamptz(6)
  notes                String?
  is_active            Boolean

  @@index([fy, target_type], map: "target_sett_fy_ed1565_idx")
  @@index([is_active], map: "target_sett_is_acti_8a4829_idx")
  @@index([scope_type, scope_id], map: "target_sett_scope_t_dce157_idx")
  @@index([created_at], map: "target_setting_created_at_123d3126")
  @@index([id], map: "target_setting_id_b3d4c5eb_like")
}

model upload_batch {
  created_at                      DateTime                          @db.Timestamptz(6)
  updated_at                      DateTime                          @db.Timestamptz(6)
  id                              String                            @id @db.VarChar(30)
  source                          String                            @db.VarChar(64)
  file_name                       String?                           @db.VarChar(512)
  uploaded_by                     String                            @db.VarChar(30)
  row_count                       Int
  accepted_count                  Int
  flagged_count                   Int
  created_rows                    Int
  duplicate_rows                  Int
  error_summary                   String?
  failed_rows                     Int
  original_file_name              String?                           @db.VarChar(512)
  skipped_rows                    Int
  status                          String                            @db.VarChar(32)
  total_rows                      Int
  updated_rows                    Int
  upload_type                     String                            @db.VarChar(16)
  school_account_owner_upload_map school_account_owner_upload_map[]
  upload_batch_row_result         upload_batch_row_result[]

  @@index([created_at], map: "upload_batch_created_at_9574a5a3")
  @@index([id], map: "upload_batch_id_f5325908_like")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model upload_batch_row_result {
  created_at      DateTime     @db.Timestamptz(6)
  updated_at      DateTime     @db.Timestamptz(6)
  id              String       @id @db.VarChar(30)
  row_number      Int
  school_id       String?      @db.VarChar(128)
  status          String       @db.VarChar(16)
  error_message   String?
  raw_data_json   Json?
  upload_batch_id String       @db.VarChar(30)
  upload_batch    upload_batch @relation(fields: [upload_batch_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "upload_batch_row_res_upload_batch_id_b4074ebe_fk_upload_ba")

  @@index([status], map: "upload_batc_status_03acec_idx")
  @@index([upload_batch_id], map: "upload_batc_upload__fff631_idx")
  @@index([created_at], map: "upload_batch_row_result_created_at_72445d81")
  @@index([id], map: "upload_batch_row_result_id_2e142576_like")
  @@index([upload_batch_id], map: "upload_batch_row_result_upload_batch_id_b4074ebe")
  @@index([upload_batch_id], map: "upload_batch_row_result_upload_batch_id_b4074ebe_like")
}

model user {
  last_login                                          DateTime?               @db.Timestamptz(6)
  is_superuser                                        Boolean
  created_at                                          DateTime                @db.Timestamptz(6)
  updated_at                                          DateTime                @db.Timestamptz(6)
  deleted_at                                          DateTime?               @db.Timestamptz(6)
  id                                                  String                  @id @db.VarChar(30)
  email                                               String                  @unique @db.VarChar(254)
  password                                            String?                 @db.VarChar(255)
  name                                                String                  @db.VarChar(255)
  phone                                               String?                 @db.VarChar(64)
  roles                                               String[]                @db.VarChar(64)
  active_role                                         String                  @db.VarChar(64)
  is_active                                           Boolean
  status                                              String                  @db.VarChar(32)
  password_set_at                                     DateTime?               @db.Timestamptz(6)
  last_login_at                                       DateTime?               @db.Timestamptz(6)
  failed_login_count                                  Int
  locked_until                                        DateTime?               @db.Timestamptz(6)
  mfa_enabled                                         Boolean
  mfa_secret                                          String?                 @db.VarChar(512)
  password_reset_token_hash                           String?                 @db.VarChar(255)
  password_reset_expires                              DateTime?               @db.Timestamptz(6)
  is_staff                                            Boolean
  environment                                         String                  @db.VarChar(16)
  source                                              String                  @db.VarChar(32)
  django_admin_log                                    django_admin_log[]
  partner                                             partner?
  refresh_token                                       refresh_token[]
  staff_profile                                       staff_profile?
  user_groups                                         user_groups[]
  user_invitation_user_invitation_invited_by_idTouser user_invitation[]       @relation("user_invitation_invited_by_idTouser")
  user_invitation_user_invitation_user_idTouser       user_invitation[]       @relation("user_invitation_user_idTouser")
  user_user_permissions                               user_user_permissions[]

  @@index([created_at], map: "user_created_at_64c716d1")
  @@index([deleted_at], map: "user_deleted_at_1b4bc546")
  @@index([email], map: "user_email_54dc62b2_like")
  @@index([environment], map: "user_environment_14f415dd")
  @@index([environment], map: "user_environment_14f415dd_like")
  @@index([id], map: "user_id_ea8f538c_like")
  @@index([source], map: "user_source_51744f77")
  @@index([source], map: "user_source_51744f77_like")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model user_groups {
  id         BigInt     @id @default(autoincrement())
  user_id    String     @db.VarChar(30)
  group_id   Int
  auth_group auth_group @relation(fields: [group_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "user_groups_group_id_b76f8aba_fk_auth_group_id")
  user       user       @relation(fields: [user_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "user_groups_user_id_abaea130_fk_user_id")

  @@unique([user_id, group_id], map: "user_groups_user_id_group_id_40beef00_uniq")
  @@index([group_id], map: "user_groups_group_id_b76f8aba")
  @@index([user_id], map: "user_groups_user_id_abaea130")
  @@index([user_id], map: "user_groups_user_id_abaea130_like")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model user_invitation {
  created_at                               DateTime  @db.Timestamptz(6)
  updated_at                               DateTime  @db.Timestamptz(6)
  id                                       String    @id @db.VarChar(30)
  token_hash                               String    @unique @db.VarChar(255)
  expires_at                               DateTime  @db.Timestamptz(6)
  accepted_at                              DateTime? @db.Timestamptz(6)
  revoked_at                               DateTime? @db.Timestamptz(6)
  invited_by_id                            String    @db.VarChar(30)
  user_id                                  String    @db.VarChar(30)
  user_user_invitation_invited_by_idTouser user      @relation("user_invitation_invited_by_idTouser", fields: [invited_by_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "user_invitation_invited_by_id_c7233d1b_fk_user_id")
  user_user_invitation_user_idTouser       user      @relation("user_invitation_user_idTouser", fields: [user_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "user_invitation_user_id_0cf8b6db_fk_user_id")

  @@index([user_id], map: "user_invita_user_id_9e4f4a_idx")
  @@index([created_at], map: "user_invitation_created_at_2f870428")
  @@index([id], map: "user_invitation_id_2e11be76_like")
  @@index([invited_by_id], map: "user_invitation_invited_by_id_c7233d1b")
  @@index([invited_by_id], map: "user_invitation_invited_by_id_c7233d1b_like")
  @@index([token_hash], map: "user_invitation_token_hash_e5cd383a_like")
  @@index([user_id], map: "user_invitation_user_id_0cf8b6db")
  @@index([user_id], map: "user_invitation_user_id_0cf8b6db_like")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model user_user_permissions {
  id              BigInt          @id @default(autoincrement())
  user_id         String          @db.VarChar(30)
  permission_id   Int
  auth_permission auth_permission @relation(fields: [permission_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "user_user_permission_permission_id_9deb68a3_fk_auth_perm")
  user            user            @relation(fields: [user_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "user_user_permissions_user_id_ed4a47ea_fk_user_id")

  @@unique([user_id, permission_id], map: "user_user_permissions_user_id_permission_id_7dc6e2e0_uniq")
  @@index([permission_id], map: "user_user_permissions_permission_id_9deb68a3")
  @@index([user_id], map: "user_user_permissions_user_id_ed4a47ea")
  @@index([user_id], map: "user_user_permissions_user_id_ed4a47ea_like")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model village {
  created_at DateTime @db.Timestamptz(6)
  updated_at DateTime @db.Timestamptz(6)
  id         String   @id @db.VarChar(30)
  name       String   @db.VarChar(255)
  source     String?  @db.VarChar(255)
  parish_id  String   @db.VarChar(30)
  parish     parish   @relation(fields: [parish_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "village_parish_id_c535e3a1_fk_parish_id")

  @@unique([parish_id, name], map: "uniq_village_parish_name")
  @@index([created_at], map: "village_created_at_c075b70b")
  @@index([id], map: "village_id_14fee182_like")
  @@index([parish_id], map: "village_parish__967e80_idx")
  @@index([parish_id], map: "village_parish_id_c535e3a1")
  @@index([parish_id], map: "village_parish_id_c535e3a1_like")
}

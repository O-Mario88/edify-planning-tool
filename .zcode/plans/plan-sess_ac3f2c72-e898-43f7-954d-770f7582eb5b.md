# Merge Leave Pages — "Leave & Personal Time Off"

## What changes
Merge 4 separate sidebar items into 1 unified page called **"Leave & Personal Time Off"** at `/personal-time-off/`. The existing PTO page already aggregates balance, requests, coverage, holidays, and conflicts — it becomes the single landing page. The other leave pages remain accessible via their URLs (for HR/approvers who need them) but are removed from the sidebar.

## 4 items being merged into 1:
1. **"Personal Time Off"** (`/personal-time-off/`) → becomes the single "Leave & Personal Time Off" item
2. **"Leave Calendar"** (`/leave/calendar`) → removed from sidebar
3. **"Leave Coverage"** (`/leave/coverage`) → removed from sidebar
4. **"Public Holidays"** (`/public-holidays`) → removed from sidebar

## Items staying in sidebar (different role scopes):
- **"Leave Approvals"** stays in "MY WORK" (PL/CD/RVP/HR only)
- **"Leave Tracker"**, **"Team Availability"** stay in "SCHOOLS & FIELD" (PL/CD/RVP/HR only)
- **"Leave Policies"** stays in "ADMINISTRATION" (HR/Admin only)

## File changes (1 file):

### `apps/core/navigation.py`
1. Rename "Personal Time Off" → "Leave & Personal Time Off" in the MY WORK group
2. Remove "Leave Coverage" from SCHOOLS & FIELD group
3. Remove "Leave Calendar" from SCHOOLS & FIELD group
4. Remove "Public Holidays" from ADMINISTRATION group

That's it — 4 lines changed in 1 file. The URLs still work for anyone who needs them directly; they're just consolidated in the sidebar.
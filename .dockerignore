node_modules
**/node_modules
.next
.git
.env
.env.*
*.log
npm-debug.log*
.DS_Store
coverage
.vscode
# The web image is built from the repo root; edify-api is a sibling service with
# its own Dockerfile and must NOT be copied into the web image.
edify-api
# Compose / deploy docs are not needed inside the web image.
docker-compose.yml
DEPLOY.md
